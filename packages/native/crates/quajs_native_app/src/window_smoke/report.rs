use serde::Serialize;

#[derive(Clone, Debug, PartialEq, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct NativeWindowSmokeReport {
    pub adapter_name: String,
    pub backend: String,
    pub surface_format: String,
    pub present_mode: String,
    pub present_status: String,
    pub presented: bool,
    pub present_attempt_count: usize,
    pub target_frame_count: usize,
    pub rendered_frame_count: usize,
    pub resize_count: usize,
    pub surface_recovery_count: usize,
    pub surface_recovery_attempt_count: usize,
    pub surface_recovery_success_count: usize,
    pub surface_recovery_missing_size_count: usize,
    pub surface_recovery_error_count: usize,
    pub device_recovery_count: usize,
    pub device_recovery_attempt_count: usize,
    pub device_recovery_success_count: usize,
    pub device_recovery_missing_size_count: usize,
    pub device_recovery_error_count: usize,
    pub present_failure_count: usize,
    pub recoverable_surface_failure_count: usize,
    pub recoverable_device_failure_count: usize,
    pub last_present_failure_kind: Option<String>,
    pub last_surface_present_failure_kind: Option<String>,
    pub last_recovery_action: Option<String>,
    pub last_surface_recovery_status: Option<String>,
    pub last_device_recovery_status: Option<String>,
    pub texture_upload_pending_request_count: usize,
    pub texture_upload_already_resident_count: usize,
    pub texture_upload_uploaded_count: usize,
    pub texture_upload_error_count: usize,
    pub texture_upload_resubmit_count: usize,
    pub resubmitted_after_texture_upload: bool,
    pub font_atlas_uploaded_count: usize,
    pub font_atlas_error_count: usize,
    pub font_atlas_text_draw_count: usize,
    pub shaped_text_draw_count: usize,
    pub bitmap_text_draw_count: usize,
    pub font_atlas_resource_ids: Vec<String>,
    pub linear_sampled_texture_bind_group_count: usize,
    pub nearest_sampled_texture_bind_group_count: usize,
    pub texture_lifecycle_sync_count: usize,
    pub texture_lifecycle_initial_sync_count: usize,
    pub texture_lifecycle_observed_bundle_count: usize,
    pub texture_lifecycle_tracked_package_count: usize,
    pub texture_lifecycle_blocked_package_count: usize,
    pub texture_lifecycle_release_attempt_count: usize,
    pub texture_lifecycle_released_package_count: usize,
    pub texture_lifecycle_texture_cleanup_error_count: usize,
    pub texture_shutdown_count: usize,
    pub texture_shutdown_released_resource_count: usize,
    pub texture_shutdown_host_cleanup_count: usize,
    pub texture_shutdown_released_count: usize,
    pub texture_shutdown_cleanup_error_count: usize,
    pub texture_shutdown_video_frame_texture_uploaded_count: usize,
    pub texture_shutdown_video_frame_texture_released_count: usize,
    pub texture_shutdown_video_frame_texture_error_count: usize,
    pub texture_shutdown_font_atlas_uploaded_count: usize,
    pub texture_shutdown_font_atlas_released_count: usize,
    pub texture_shutdown_font_atlas_error_count: usize,
    pub audio_backend_applied_plan_count: usize,
    pub audio_backend_applied_command_count: usize,
    pub audio_backend_active_track_count: usize,
    pub audio_backend_peak_active_track_count: usize,
    pub video_backend_loaded_asset_count: usize,
    pub video_backend_resident_asset_count: usize,
    pub video_backend_applied_plan_count: usize,
    pub video_backend_applied_command_count: usize,
    pub video_backend_active_stream_count: usize,
    pub video_backend_decoded_stream_count: usize,
    pub video_backend_decode_failure_count: usize,
    pub video_backend_missing_asset_count: usize,
    pub video_backend_published_frame_count: usize,
    pub video_backend_pending_frame_count: usize,
    pub video_backend_released_texture_count: usize,
    pub video_backend_pending_release_count: usize,
    pub pointer_event_count: usize,
    pub pointer_dispatch_count: usize,
    pub pointer_intent_emit_count: usize,
    pub pointer_probe_count: usize,
    pub pointer_cancel_count: usize,
    pub pointer_last_intent_type: Option<String>,
    pub focus_gain_count: usize,
    pub focus_loss_count: usize,
    pub focus_intent_emit_count: usize,
    pub keyboard_event_count: usize,
    pub keyboard_press_count: usize,
    pub keyboard_release_count: usize,
    pub keyboard_repeat_count: usize,
    pub keyboard_intent_emit_count: usize,
    pub ime_event_count: usize,
    pub ime_enabled_count: usize,
    pub ime_disabled_count: usize,
    pub ime_preedit_count: usize,
    pub ime_commit_count: usize,
    pub ime_intent_emit_count: usize,
    pub ime_composition_active: bool,
    pub ime_last_text_byte_count: Option<usize>,
    pub ime_last_cursor_start: Option<usize>,
    pub ime_last_cursor_end: Option<usize>,
    pub app_lifecycle_state: String,
    pub app_focused: bool,
    pub app_visible: bool,
    pub app_redraw_pending: bool,
    pub app_resume_count: usize,
    pub app_suspend_count: usize,
    pub app_visibility_change_count: usize,
    pub app_lifecycle_tick_request_count: usize,
    pub app_redraw_request_count: usize,
    pub last_resize_physical_width: Option<u32>,
    pub last_resize_physical_height: Option<u32>,
    pub logical_width: f64,
    pub logical_height: f64,
    pub physical_width: u32,
    pub physical_height: u32,
    pub device_pixel_ratio: f64,
    pub revision: u64,
    pub pass_count: usize,
    pub batch_count: usize,
    pub command_count: usize,
    pub submitted_command_buffer_count: usize,
    pub frame_capture_mime_type: Option<String>,
    pub frame_capture_byte_count: usize,
    pub frame_capture_width: Option<u32>,
    pub frame_capture_height: Option<u32>,
    pub frame_capture_png_signature_valid: bool,
    pub frame_capture_visible_pixel_count: usize,
    pub frame_capture_colored_pixel_count: usize,
}
