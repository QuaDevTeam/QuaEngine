{
  "name": "store",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/store/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["logger:build", "utils:build"],
      "options": {
        "command": "vite build",
        "cwd": "packages/store"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite build --watch",
        "cwd": "packages/store"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint .",
        "cwd": "packages/store"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"Error: no test specified\" && exit 1",
        "cwd": "packages/store"
      }
    }
  },
  "tags": []
}