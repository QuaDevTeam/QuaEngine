# create-qua-game

Project scaffolding CLI for QuaEngine visual novel games.

```bash
pnpm create qua-game my-game
```

The first template is `visual-novel-vue`, a Vue + Vite starter wired to QuaEngine, QuaScript, the Vue renderer preset, Vite dev assets, and Quack asset builds.
