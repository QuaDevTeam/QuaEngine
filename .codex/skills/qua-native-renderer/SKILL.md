---
name: qua-native-renderer
description: Use, implement, or review QuaEngine native renderer work under packages/native, including native host bridge, native contracts, assets/store adapters, QuickJS runtime, wgpu renderer, QUI/QSS, target plugin isolation, and native package compatibility.
---

# Qua Native Renderer

Use this skill for `packages/native/*`, Rust native runtime/renderer crates, native host contracts, native asset/store adapters, QUI/QSS native authoring, and native packaging.

## Boundaries

- Native renderer is a projection layer only.
- Engine/store remain authoritative for game state, save/load, settings, audio intent, UI overlays, runtime packages, and plugin state.
- Communication uses `@quajs/pipeline` and render-core/plugin projections; do not add a second event bus.
- Runtime content remains Quack-built QPK packages. Dynamic QPKs may contain QS/JS modules and resources only, never native code.
- Native runtime package guards must scan script/plugin/scene/migration asset references, their runtime module variants, emitted bundle asset names, paths, relative paths, variant names/paths, and declared UI/media/resource metadata references for empty or suffix-only names, absolute/remote/traversal references, and forbidden native payload extensions before QuickJS evaluation. Resource metadata scanning is intentionally scoped to resource-like fields such as `assets`, `resources`, `surface`, `style`, `tokens`, `qui`, `qss`, `audio`, `video`, `image`, `poster`, `fallbackImage`, `assetName`, `module`, `path`, `relativePath`, `src`, `name`, `url`, `uri`, and `href`; ordinary compatibility strings such as package names, version ranges, capability ids, QSS feature names, and QUI component names must not be treated as package asset references. Guards must also reject package-level and plugin-level native-code declarations such as `nativeBinaries`, `nativePayloads`, `nativeEntry`, or truthy `nativeCode`, not only compatibility metadata. Package-level and plugin-level native renderer compatibility blocks (`metadata.nativeRenderer` and `metadata.renderers.native`) must explicitly declare `nativeCode: false`; missing or truthy values are blockers before signature verification.
- Native version/capability data comes from the signed native app/Rust build and is exposed through `QuaNativeHostInfo`; QPK content cannot override it.
- Rust native startup must fill `QuaNativeHostInfo.runtime.quickjsVersion` from `quickjs_runtime_version()`. Builds without an installed QuickJS evaluator report `unsupported`; do not leave the startup host info at `pending` or claim a real QuickJS engine version before a backend exists.
- Native `target-bundle-manifest.json` files must record `nativeRuntime.quickjsVersion`, `nativeRuntime.nativeRuntimeVersion`, `nativeRuntime.assetAdapterVersion`, and `nativeRuntime.storeAdapterVersion` from the packaged native host/adapters, plus the exact native renderer capability id set from the signed host renderer manifest. `@quajs/engine-native` and Rust native startup must compare runtime versions and the renderer capability id set against `QuaNativeHostInfo` before exposing the native host plugin as initialized, so a stale or under-declared manifest cannot start QuickJS or renderer work against a different signed native runtime.
- Quack native artifact plans must prove their Rust QuickJS build mode before emitting a native target-bundle manifest. If `nativeRuntime.quickjsVersion` is anything other than `unsupported`, the plan's normalized `targets.native.build.cargoFeatures` must include `quickjs-rquickjs`; no-QuickJS builds may emit `quickjsVersion: "unsupported"` without that Cargo feature. This keeps release/debug manifests from claiming real QuickJS/QS support when the signed native app was built with default unsupported runtime features.
- Native runtime package compatibility may be declared as `metadata.nativeRenderer` or target-scoped `metadata.renderers.native`; checks normalize `renderer`/`rendererPackage` to `packageName`, `version`/`rendererVersion` to `versionRange`, and `capabilityIds`/`optionalCapabilityIds` to capability requirement lists before activation. Required `assetKinds`, `intentEvents`, `qssFeatures`, and `quiComponents` must be checked against host renderer capabilities before activation; optional asset kinds, optional intent events, optional QSS features, and optional QUI components may warn and fall back without blocking activation. Native compatibility blocks must explicitly declare `nativeCode: false`; missing or truthy values are rejected before signature verification or script evaluation. Dynamic UI package builders and third-party native renderer entries should use `createNativeUiSurfaceCompatibility` or `@quajs/native-ui-compiler`'s `createNativeUiSurfaceCompatibilityFromDocuments` so `native-wgpu.ui.surface@1`, `qui` / `qss` / `tokens`, renderer package metadata, document-derived intent events / QSS features / QUI components / asset kinds, and `nativeCode: false` stay consistent with the actual renderer capability manifest without inventing undeclared audio/video capabilities.
- Native dynamic module loading must use `createNativeRuntimeModuleLoader` or an equivalent restricted loader. It may load only runtime-package-declared, package-relative JavaScript module-like `assetName` script assets (`.js`, `.mjs`, or `.cjs`) through QuaAssets/native host bytes, then pass code to a trusted Rust/QuickJS evaluator. Runtime package scripts may explicitly declare package-local QuickJS dependencies through `metadata.nativeQuickJs.imports`; those imports are loaded into a same-package/same-bundle module graph and are the only relative JS imports the Rust evaluator may resolve. The loader itself must reject empty or suffix-only names, backslash paths, absolute paths, URLs/URI schemes, traversal, unsafe runtime module variant asset/module references, unsafe module graph entries, and native payload-looking extensions before asset lookup as defense in depth, even though trust policy guards run before activation. Do not load runtime modules from filesystem paths, URLs, Node resolution, Web `Blob`, or dynamic `import()`.
- Rust `quajs_native_runtime` QuickJS request validation must mirror the restricted TS loader and shared contracts helper: package-relative asset names only, JavaScript module-like extensions only (`.js`, `.mjs`, `.cjs`) after stripping inert `?query` / `#hash` suffixes, explicit native-payload rejection after stripping the same suffixes, module bytes and UTF-8 code bytes capped by `maxModuleBytes`, and no filesystem/URL/native payload escape hatches before evaluator dispatch.
- Rust `quajs_native_runtime` exposes the real rquickjs backend behind the opt-in Cargo feature `quickjs-rquickjs`. Default builds still report `quickjsVersion: "unsupported"` and use `UnsupportedQuickJsModuleEvaluator`; feature builds report `quickjs-<JS_GetVersion>;rquickjs-0.12.1`, evaluate ES modules from already-validated package script bytes, apply heap/stack/tick limits, keep persistent QuickJS namespace handles, and drop those handles through the evaluator release hooks when namespace/package release APIs run. This backend proves native QuickJS module evaluation, namespace lifetime management, JSON-safe export calls, GameStep factory/run-handle projection including async factories whose Promise resolves inside QuickJS without a host continuation, a controlled StepContext command bridge for already-evaluated script module namespaces, `ctx.engine.waitFor` suspension/resume through a host continuation handle, `ctx.t` suspension/resume through a real TS `StepContext.t` translation continuation, `ctx.pipeline.emit` suspension/resume through the real TS pipeline, long-lived `ctx.pipeline.on/off` listener registration through `pipelineSubscriptions` plus `dispatchQuickJsPipelineListener`, a restricted same-runtime built-in helper resolver for compiled QuaScript imports from `@quajs/engine` (`resolveQuaText`, `RenderToLogicEvents`, and story target/asset helpers such as `node`, `label`, `scene`, `script`, `packageNode`, `checkpoint`, and `image`), host-side helper-call continuations for all game-facing `@quajs/character` helpers and official decorator runtime helper imports from `@quajs/character/animation`, `@quajs/plugin-achievement`, `@quajs/plugin-animation`, `@quajs/plugin-audio`, `@quajs/plugin-background`, `@quajs/plugin-backlog`, `@quajs/plugin-gallery`, `@quajs/plugin-inventory`, and `@quajs/story-graph`, and package-local relative JS imports only when the TS loader included them in the explicit same-package/same-bundle module graph from `metadata.nativeQuickJs.imports`. The TS product host must register character helpers through `quickJsHelperModules` so the real character registry resolves ids, aliases, and sprite keys; Rust must return `pendingHelperCall` and must not synthesize character state. Long-lived listener callbacks may issue JSON-safe engine commands and may register/unregister listeners; they must not suspend on `ctx.engine.waitFor`, `ctx.t`, `ctx.pipeline.emit`, or helper-call continuations inside the listener callback.
- `quajs_native_app` exposes an opt-in JSONL native host bridge when built with `--features quickjs-rquickjs` and launched with `QUA_NATIVE_QUICKJS_BRIDGE=1`. This mode is exclusive and writes only `NativeHostApiResponse` JSON lines to stdout so TS product smoke tests can wrap it with `createNativeHostApiFromBridge`, load QPK bytes through QuaAssets, and drive real Rust/rquickjs evaluation without adding a second protocol. When `QUA_NATIVE_TARGET_BUNDLE_MANIFEST` is set, the bridge must load and validate it before constructing host info, initializing rquickjs, or serving JSONL requests, using the same native renderer/runtime version and target-isolation gates as normal native app startup. Do not use this bridge to load filesystem scripts or bypass QPK trust/module guards; it only dispatches the existing `NativeHostApiRequest` wire contract.
- `quajs_native_app` exposes an opt-in headless JSONL native product bridge when built with `--features image-decode` and launched with `QUA_NATIVE_PRODUCT_BRIDGE=1`. This mode is exclusive with `QUA_NATIVE_QUICKJS_BRIDGE`, validates `QUA_NATIVE_TARGET_BUNDLE_MANIFEST` before constructing host info or the runtime, owns one long-lived `NativeProductRuntime`, accepts already-resolved `NativeRendererJsonFrameInput` payloads only, supports lifecycle ticks, shutdown, and transient renderer intent draining, and returns renderer/product summaries plus drained `NativeRendererIntent[]` so TS can forward them through the existing pipeline. It must not load filesystem scripts/assets, mount loose resources, decide scene progression, or become a second event bus; it is a bridge for an engine-owned TS product shell to drive Rust projection/runtime work, not complete OS app-loop parity by itself.
- `@quajs/native-contracts` exposes the typed TS-side product bridge adapter (`createNativeProductBridgeFromBridge`) and product bridge request/response summary contracts for TS shells that drive `QUA_NATIVE_PRODUCT_BRIDGE`. Keep this adapter separate from `QuaNativeHostApi` so product-frame/lifecycle/shutdown commands do not become low-level host capabilities.
- `@quajs/engine-native` feature UI uses explicit `NativeRendererFeatureSurfaceEntry` registrations. Each entry reads one engine-owned `view.plugins[pluginId]` projection, emits resolved render-only native overlays, and declares an allowlisted mapping from surface action ids to the owning plugin's existing pipeline events. Pass the same entry list to native frame serialization and `NativeHostPlugin`; do not infer entries from arbitrary projection keys, dispatch unregistered actions, or turn generic `ui/intent` metadata into an unrestricted event emitter.
- Native QuickJS export calls use the same host bridge through `callQuickJsModuleExport(moduleNamespaceId, exportName, argsJson)`. This bridge is intentionally JSON-safe: `argsJson` must be a JSON array, results are returned as `valueJson`, and prototype-related export names are blocked. `@quajs/engine-native` may build a JSON-safe namespace proxy from this API for simple exports. Script modules should use the GameStep bridge (`callQuickJsGameStepFactory` plus `callQuickJsGameStepRun`/`resumeQuickJsGameStepRun`) instead of JSON export calls when the export returns compiled QuaScript `GameStep[]`: Rust calls the factory, serializes each step's `uuid` and JSON metadata, stores persistent `run` function handles, lets compiled QS import only registered built-in, official helper bridge modules, or explicitly declared package-local module graph imports, injects a local QuickJS `ctx.engine` projection that records allowlisted JSON-safe engine commands, including dialogue/choice/view, flow-control, save/load, and rollback decorator commands, plus local `ctx.t`, `ctx.pipeline.emit`, `ctx.pipeline.on/off`, and official helper-call continuation hooks, returns those descriptors for `@quajs/engine-native` to execute against the real TS `StepContext.engine` / `StepContext.pipeline` / registered helper modules, and suspends on `ctx.engine.waitFor` with `pendingWait`, `ctx.t` with `pendingTranslation`, `ctx.pipeline.emit` with `pendingPipelineEmit`, or official helper imports with `pendingHelperCall`. `@quajs/engine-native` must execute returned commands, apply returned `pipelineSubscriptions` through the TS subscription bridge, dispatch listener callbacks through `dispatchQuickJsPipelineListener`, wait through the real `ctx.engine.waitFor(event)`, translate through the real `ctx.t(key, options)`, emit through the real `ctx.pipeline.emit(event, payload)`, execute registered helper functions with the real `ctx.engine`, pass JSON-safe resume payloads to `resumeQuickJsGameStepRun`, and repeat until the QuickJS promise completes. Arbitrary store/assets access and undeclared package-local imports must fail clearly instead of being faked. Product smoke covers Quack-emitted native manifests, valid/stale startup gates, real character registry and animation helpers, fixed-time native animation projection, a golden Rust command graph, async JS script factories, compiled `.qs` content packaged as a QPK script asset with explicit same-package `metadata.nativeQuickJs.imports`, real `ctx.t` translation, real `ctx.pipeline.emit`, long-lived `ctx.pipeline.on/off` listener dispatch, background/audio helper continuations, engine-owned audio projection serialization, dialogue/choice pipeline events, native renderer JSON facade rendering, package-local achievement/gallery/backlog/settings native feature surfaces, and runtime-package unload cleanup of package-owned QuickJS namespaces through `NativeHostPlugin`. Demo native smoke selects each feature panel through `--panel=<feature>`, validates the expected resolved surface key, then runs the Rust/WGPU window smoke. Do not claim complete native product parity until Rust-owned real app loop behavior and native media backends are closed.
- `@quajs/native-contracts` owns `validateNativeQuickJsEvaluationRequest` / `assertNativeQuickJsEvaluationRequest` for the shared QuickJS wire boundary. `@quajs/engine-native` must call this assertion immediately before `host.evaluateQuickJsModule`, so manually constructed evaluation requests are blocked before reaching Rust even when they bypass the higher-level runtime module loader.
- When using the native host QuickJS bridge, `moduleNamespaceId` is only an opaque Rust/QuickJS namespace handle. `@quajs/engine-native` must use an explicit namespace resolver before returning a real engine module namespace object; do not fake module exports from the id string.
- Native host QuickJS cleanup APIs (`releaseQuickJsModuleNamespace`, `releaseQuickJsPackageNamespaces`, `getQuickJsNamespaceSummary`, and `getQuickJsPackageNamespaceSummary`) are resource-ledger APIs only. Real hosts must use a persistent `QuickJsModuleNamespaceRegistry` across evaluation/release calls, not a per-request temporary registry, so runtime package unload can release package-owned namespace handles and check memory summaries. QuickJS namespace summaries must expose `moduleBytes`, `codeBytes`, and `totalBytes` so dynamic-package unload checks, dashboards, and benchmarks can compare total retained JS memory without recomputing it differently at each caller.
- `@quajs/engine-native` must release package-owned QuickJS namespaces from the late renderer-facing runtime-package unload event (`LogicToRenderEvents.RUNTIME_PACKAGE_UNLOAD`), after runtime engine plugins have been unloaded. It should call `releaseQuickJsPackageNamespaces(packageId)` when the host supports it, so successful QuickJS namespace handles do not outlive their owning Runtime QPK without being released before plugin teardown. Cleanup failures must be recorded and reported as recoverable `render/error` events with phase `quickjs-cleanup`; they must not block or rethrow from the runtime-package unload pipeline.
- Rust `quajs_native_app` product loop ownership is split by layer: `NativeProductLoop` owns renderer/host lifecycle sync, `NativeProductWindowLoop` owns surface present/resize/recovery policy, and `NativeProductAppLoop` owns transient app lifecycle/redraw scheduling such as resumed, suspended, focus, occlusion, idle lifecycle ticks without new frames, and redraw retry requests. Window smoke and future shells should translate platform events into those product-loop actions instead of directly deciding continuous redraw or host lifecycle policy.
- Native window development uses `QUA_NATIVE_RENDERER_WINDOW_DEV=1` for a long-lived WGPU window and `QUA_NATIVE_RENDERER_WINDOW_DEV_QPK=<path>` for its Quack-built asset bundle. The development QPK loader may mount validated uncompressed/unencrypted QPK bytes into the in-memory host, but it must reject unsafe paths and native payloads and must not add loose filesystem asset loading. Texture, audio, font, and video host sync should preserve legacy logical asset-name reads and then fall back to Quack's canonical `assets/{assetType}/{assetName}` QPK URL while preserving package-candidate order.
- Demo native development status UI belongs on the low `hud` overlay stack below product feature surfaces. Settings, backlog, gallery, achievement, and other engine/plugin overlays must cover the dev status panel instead of being obscured by debug chrome; ordinary scene captures may still show it.
- WGPU buffer assembly stores indices local to each draw call and supplies that draw's `first_vertex` as the WGPU base vertex. Do not offset both the packed indices and `first_vertex`; doing so corrupts every non-first image/text draw in a batch even when command-graph smoke metrics pass.
- When dialogue and choices are visible together, native choice bounds must be resolved above the dialogue panel in logical stage coordinates. Layout tests must assert the two projected regions do not overlap.
- Native demo smoke must parse the machine-readable window report and fail unless the surface presents, the render graph submits non-empty work, QPK textures reach the WGPU resident set and are released at shutdown, texture errors remain zero, and logical/CSS/device-pixel dimensions are consistent. A process exit code alone is not sufficient visual-rendering coverage.
- Native scene changes use `NativeSceneTransitionController` inside `NativeHostPlugin`. The controller owns only transient timing, listens to `scene/change` through the existing pipeline, requests product redraws through the injected `requestRender` callback, exposes a read-only transition snapshot for native frame serialization, cancels replaced transitions, and emits `scene/ready` after instant or completed transitions so engine `waitForRenderer` cannot time out. Rust/WGPU consumes `view.sceneTransition` in logical stage coordinates through `native-wgpu.scene-transition@1`; it must not decide scene progression. `NativeHostPlugin.createRendererJsonFrameInput` is the product-shell frame path that automatically includes the active snapshot. Demo smoke uses `pnpm --filter demo native:smoke:transition` to verify the resolved transition reaches the real WGPU window path.
- Native frame serialization must project engine-owned `view.effects` through render-core `projectEffect` so `effect:<id>` animation tracks are resolved at the same deterministic frame time as background, character, dialogue, choices, UI, and audio. `native-wgpu.effects@1` consumes that projection: WGPU renders `fade_in`, `fade_out`, and `flash` as full logical-stage screen overlays, applies `shake` translation/scale to the selected logical target commands, preserves Runtime QPK provenance, and ignores malformed or unsupported effects without becoming effect-state authority. Effect duration/cleanup stays engine/plugin-owned; native must not invent a second lifecycle timer. Demo smoke uses `pnpm --filter demo native:smoke:effects`.
- Native dialogue typewriter uses `NativeDialogueTypewriterController` as transient renderer state. `NativeHostPlugin.createRendererJsonFrameInput` applies it after engine-owned dialogue and animation projection, slices plain/rich text by grapheme clusters, preserves rich-text structure/style, and requests redraws without writing visible-character counts into engine state. The native renderer intent bridge must let the controller consume a pressed `user/input_command` `advance` when `revealOnAdvance` reveals remaining text; only the following advance may reach engine progression. Demo smoke uses `pnpm --filter demo native:smoke:typewriter`. Typewriter sound playback is not part of this capability yet and must not be claimed until it is connected to a real package-aware native audio backend.
- Native keyboard, pointer, focus, and IME/text input must emit user intent through `NativeHostApi.emit_renderer_intent` and the existing `@quajs/pipeline` render-to-logic bridge. IME preedit/commit/enabled/disabled events use the standard `RenderToLogicEvents.USER_TEXT_INPUT` (`user/text_input`) payload with transient text/cursor metadata; ordinary printable `KeyEvent.text` key presses may emit `user/text_input` commit payloads after command-key handling filters out shortcuts/control characters. Rust product input may report byte-count summaries for smoke metrics but must not retain authoritative text-entry state.
- `native-wgpu.input.pointer@1` covers pointer-dispatched `choice/select` and `ui/intent` only. Text/IME support is declared separately as `native-wgpu.input.text@1` with `intentEvents: ["user/text_input"]`, so native package compatibility can require text input without pretending it depends on pointer hit-testing or UI surface components.
- Rust `quajs_wgpu_renderer` consumes resolved QUI/QSS projection data only. QSS parsing, selector matching, cascade, inheritance, and language-server diagnostics belong in TS/compiler/tooling packages, not in the renderer.
- Native QSS supports one structured outer `box-shadow` or `text-shadow` value in the form `offset-x offset-y [blur-radius] [spread-radius] color`, plus `none`. The TS compiler must reject inset and multiple shadows and emit resolved shadow objects; Rust validates and consumes that IR without parsing QSS. WGPU may approximate blur with layered rounded quads or sampled text draws, but must not claim browser Gaussian blur, backdrop blur, inset shadow, or multiple-shadow parity.
- Rust `quajs_wgpu_renderer` projection DTOs serialize/deserialize the native bridge JSON boundary with camelCase fields. Native UI surface node `kind` values must stay aligned with foundational QUI registry component names such as `Box`, `Text`, and `Button`; the JSON facade must reject missing, null, non-string, or unsupported resolved surface node `kind` fields instead of silently defaulting to `Box` or deferring unknown composite names to serde parse errors. Do not add Rust-side parsing of QUI/QSS authoring syntax to make up for malformed projection JSON.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved native UI surface nodes to explicitly provide `visible` before serde defaults can apply. `@quajs/native-ui-compiler` must emit `UiSurfaceNodeProjection.visible` for every projected node, using `true` when no QUI `show`, QSS `display: none`, or QSS `visibility: hidden` rule overrides it. Direct Rust DTO builders may still default node visibility for internal tests, but native app / QuickJS bridge JSON must not silently turn malformed resolved QUI/QSS projection nodes into visible UI.
- Rust `quajs_wgpu_renderer` exposes `NativeRendererJsonFrameInput` and JSON facade methods for native app / QuickJS bridge ingestion of already-resolved layout, container, and view projection data. These helpers may deserialize camelCase bridge JSON and call existing frame preparation/rendering paths, but they must not parse QUI/QSS authoring syntax or become renderer-owned game state.
- Rust `quajs_wgpu_renderer` JSON facade must require the top-level resolved `view` projection to be explicitly present before serde defaults apply. The native app / QuickJS bridge must not accept `{}` or layout-only JSON as an empty valid frame, because that can hide a missing engine projection and incorrectly advance renderer state.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe native bridge stage inputs before frame preparation. Validate `NativeRendererJsonFrameInput.layout` and `.container` fields for finite positive dimensions, finite positive bounded aspect ratios, non-inverted aspect intervals, finite positive bounded `devicePixelRatio`, and finite non-negative bounded safe-area insets. The lower-level stage layout resolver may normalize internal caller values, but the JSON bridge must fail malformed app / QuickJS input instead of silently falling back.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe package asset references before frame preparation, backend submission, or audio planning. Validate known projection `assetName` fields and declarative UI surface `key` fields for empty or suffix-only names, URLs/URI schemes, absolute paths, backslash paths, traversal, and native payload-looking extensions after stripping `?query` / `#hash` suffixes. Validate known projection `assetType` fields as safe native asset-kind identifiers so resolved JSON cannot inject paths, URL schemes, or empty resource namespaces into renderer resource IDs. Validate resolved dialogue/UI `fontFamily` names because they become `fonts:*` resource ids; they may contain normal font-name spaces but must not be empty, URL/scheme-like, path-like, traversal-like, query/hash-suffixed, namespace-separated with `:`, control-character-bearing, or native payload-looking. Validate resolved dialogue/UI color fields (`RichTextStyle.color`, `UiSurfaceResolvedStyle.backgroundColor`, `borderColor`, and `color`) as safe native color literals only: hex, `rgb(...)`, `rgba(...)`, `transparent`, `currentColor`, or basic named colors. Rust should defensively reject malformed resolved JSON, but it must not parse QSS or infer fallback meaning for invalid color syntax.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved image resource projections to explicitly provide `assetType` before serde defaults can apply. This includes `DialogueAvatarProjection.avatar.assetType`, `UiSurfaceNodeProjection.image.assetType`, and `UiSurfaceResolvedStyle.backgroundImage.assetType` in native app / QuickJS bridge JSON. Direct Rust DTO builders may still default image asset type for internal tests, but bridge JSON must not silently turn malformed resolved UI/dialogue resources into `images:*` resource ids.
- Rust `quajs_wgpu_renderer` JSON facade must also reject unsafe package provenance ids before frame preparation, because `contentPackageId` and `requiredRuntimePackages` flow into draw commands, resource ownership, memory metrics, and package unload blockers. Validate known projection provenance fields for empty/whitespace ids, URL/URI schemes, absolute/path-like ids, traversal markers, query/hash suffixes, control characters, and non-identifier characters; Rust should reject malformed resolved JSON rather than letting unsafe package ids pollute renderer ledgers.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe non-UI projection identity fields before frame preparation. Validate background layer `id`, character `id`, choice `id`, and audio track `id` with the same safe native identifier rules used for UI dispatch identifiers, and require each of those ids to be unique within its owning frame scope. These ids become draw command ids, renderer intents, resource ids, audio backend track ids, or memory/resource ledger identities.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved UI dispatch identifiers before frame preparation. Validate overlay `elementId`, overlay `overlayStack`, scene shell `overlay.overlayStack`, UI scene `id`, UI surface node `id`, intent `action`, canonical `choiceId`, and UI intent metadata keys for empty values, surrounding whitespace, control characters, dangerous URL/native/shell/FFI-like URI schemes, query/hash suffixes, absolute/path-like values, traversal markers, native payload-looking suffixes, and unsafe characters. Overlay `elementId` values must be unique within a frame's UI overlay list, UI scene `id` values must be unique within a frame's UI scene list, and UI surface node `id` values must be unique within each resolved surface tree, because these fields become draw command ids, hit-test element ids, scene/surface identities, overlay grouping/sort keys, or renderer intent dispatch payloads. Malformed resolved JSON must fail before command graph construction.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved UI intents to explicitly provide `event` before serde defaults can apply. Direct Rust DTO builders may still default `UiIntentProjection.event` for internal tests, but native app / QuickJS bridge JSON must not silently default a malformed intent to `ui/intent`.
- Rust `quajs_wgpu_renderer` JSON facade must cap resolved UI intent metadata before frame preparation because it is copied into native host `NativeRendererIntent.payloadJson`. Metadata keys must pass the safe dispatch identifier rules, and metadata payloads must stay within native renderer limits for entry count, total estimated bytes, string bytes, array/object fanout, and nesting depth. This is only a resolved payload guard; Rust must not parse business-specific action semantics or mutate engine state from metadata.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved UI geometry before frame preparation. Require every resolved native UI surface node to explicitly provide `bounds` as an object plus `bounds.x`, `bounds.y`, `bounds.width`, and `bounds.height` before serde defaults can apply. Validate those coordinates and scroll offsets as finite logical values within native renderer limits, and validate `bounds.width` / `bounds.height` as finite, non-negative logical dimensions within native renderer limits. This is a resolved-projection guard only; it must not add Rust-side QUI/QSS layout parsing or renderer-owned layout state.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved UI style numbers before frame preparation. Validate node `opacity`, style `opacity`, `backgroundPosition`, `borderRadius`, `borderWidth`, `fontSize`, `letterSpacing`, `lineHeight`, and `padding` as finite values inside the native renderer limits, with opacity/background positions normalized to `0..=1` and logical style values non-negative. Rust must reject malformed resolved JSON rather than silently clamping or reinterpreting QSS semantics.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved UI text payloads before frame preparation. Validate `UiSurfaceNodeProjection.text` for native renderer text payload limits and unsupported control characters before it enters button/text draw params or future glyph layout. This is a resolved bridge guard only; QUI content-model validation, localization policy, text interpolation, and rich text authoring semantics remain in TS compiler/tooling.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved dialogue projections to explicitly provide `visible` and `mode` before serde defaults can apply. Direct Rust DTO builders may still default `DialogueProjection.visible` and `.mode` for internal tests, but native app / QuickJS bridge JSON must not silently default an omitted dialogue visibility or mode because that can hide a missing engine/compiler projection field and change hidden-vs-visible or narration-vs-say rendering semantics.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved choice projections to explicitly provide `view.choices.visible`, `view.choices.choices`, and each choice `enabled` field before serde defaults can apply. Direct Rust DTO builders may still default choices to visible/enabled for internal tests, but native app / QuickJS bridge JSON must not silently turn omitted choice visibility or enabled state into visible, clickable UI.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved character projections to explicitly provide each character `visible` field before serde defaults can apply. Direct Rust DTO builders may still default character visibility for internal tests, but native app / QuickJS bridge JSON must not silently turn omitted character visibility into visible sprites.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved UI projections to explicitly provide `view.ui.visible` before serde defaults can apply. Direct Rust DTO builders may still default UI visibility for internal tests, but native app / QuickJS bridge JSON must not silently turn an omitted UI visibility field into visible overlays.
- Rust `quajs_wgpu_renderer` JSON facade must reject malformed resolved background projection shapes before serde parsing or frame preparation. If present, `view.background` must be an object, `view.background.mode` must be explicit, `view.background.layers` must be an array, every layer item must be an object with explicit `id` and `assetName`, and `view.background.video` must be an object with explicit `assetName`. This is only a resolved bridge guard; Rust still must not parse background authoring syntax, QSS, or renderer-owned layout state.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved dialogue/rich-text payloads before frame preparation. Validate plain dialogue text, speaker text, rich-text span text, and the aggregate `rich_text_to_plain_text` byte length for native renderer text payload limits and unsupported control characters before they enter `TextDrawParams` or future glyph layout. This is a resolved bridge guard only; dialogue authoring policy, localization, interpolation, and rich-text syntax semantics remain in engine/compiler/tooling.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved z-order values before frame preparation. Validate background layer `zIndex`, character `layer`, UI overlay / scene overlay `stackPriority` and `zIndex`, and UI surface node `zIndex` against native renderer limits before they enter overlay sorting, render graph z ordering, hit testing, or command generation. Internal render graph sorting should still use saturating arithmetic as defense in depth, but malformed bridge JSON must fail instead of relying on saturation.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved dialogue/rich-text numbers before frame preparation. Validate `RichTextStyle.fontSize` and `lineHeight` on speaker style, document style, and span style as finite positive logical values inside native renderer limits. These values become text draw parameters and font resource planning context, so malformed resolved projection must fail before command construction rather than falling back to default typography.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved background/video numbers and origins before frame preparation. Validate background and layer `x` / `y`, optional `width` / `height`, `scale`, `rotation`, and `opacity`, plus video projection `opacity`, as finite values inside native renderer limits; dimensions must be non-negative, scale must be greater than 0, rotation must be finite and bounded, and opacity must be normalized to `0..=1`. Validate background, layer, and video `origin` strings as resolved native origin values only: `left|center|right`, `top|center|bottom`, or `0%..100%` percentage axes in one-axis or two-axis form. URL, path, traversal, URI-scheme, empty, overlong, control-character, or out-of-range percentage origins must fail before command construction. The facade must also reject unsupported background video playback-state aliases such as `playbackState`, `state`, and `playing`; current native background video accepts engine-owned `loop`, `muted`, `volume`, `playbackRate`, `seekMs`, and `offsetMs` projection metadata only. This is a resolved projection guard for background layout, image/layer transform projection, and video poster/fallback projection only; it must not parse QSS, silently clamp malformed projection values, or declare real video decode support.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved character numbers before frame preparation. Validate character position `x` / `y`, `xPercent` / `yPercent`, optional `width` / `height`, `scale`, `rotation`, and character `opacity` as finite values inside native renderer limits; dimensions must be non-negative, scale must be greater than 0, and opacity must be normalized to `0..=1`. This is a resolved projection guard for character draw command planning only; Rust must not silently fall back from malformed character projection values that should have been rejected by tooling or engine projection.
- Rust `quajs_wgpu_renderer` JSON facade must reject unsafe resolved audio numbers before frame preparation. Validate audio track `volume` as a finite normalized `0..=1` value and audio track memory estimate fields (`bufferCpuBytes`, `streamCpuBytes`, `handleCpuBytes`) against native renderer limits before they enter backend command planning, memory metrics, or package unload blockers. This is still renderer-local projection/resource validation, not a real audio playback capability declaration.
- Rust `quajs_wgpu_renderer` JSON facade must require resolved native audio tracks to explicitly provide `id`, `kind`, `assetName`, `assetType`, `loadMode`, and `playbackState` before serde defaults or missing-field parse errors can apply. Direct Rust DTO builders may still default optional values for internal tests, but native app / QuickJS bridge JSON must not silently default a voice/sfx/ambient track to `bgm` resources, default an omitted load strategy to `buffered`, default an omitted playback intent to `playing`, or leave bridge identity/resource fields to generic serde errors.
- Rust `quajs_wgpu_renderer` JSON facade must reject Web/audio projection aliases in resolved native JSON before serde can ignore them. Audio tracks must use native bridge fields such as `assetName`, `playbackState`, and `looped`; `assetKey`, `state`, `loop`, and `playing` are unsupported resolved JSON fields and must fail before frame preparation instead of being silently defaulted or reinterpreted.
- Rust `quajs_native_app` may use `QUA_NATIVE_RENDERER_SMOKE_FRAME` to read an already-resolved projection JSON fixture and submit it through `NativeRendererJsonFrameInput` plus `NullNativeRenderBackend` and `NullNativeAudioBackend` as a debug/CI smoke check. It should emit both a human-readable summary and a machine-readable `Qua native renderer smoke json: ...` line with missing-resource, fallback, fallback-by-owner-package / fallback-by-required-package, texture upload request/sync, declarative resource, total memory, memory-by-kind, memory-by-package, declarative package memory, audio package memory, audio resource, active audio track metrics, and `audioBackend` plan/command/active-track metrics from the null backend diagnostics. Texture sync smoke fields must include pending, resident, and orphaned resident texture counts from `NativeRendererFrameResult.texture_upload_sync`; these are host-facing upload readiness metrics. `quajs_native_app::texture_sync` owns the host/app bridge from pending texture requests to package-scoped `NativeHostApi::read_asset_bytes` reads and an upload sink, with a feature-gated real WGPU sink that decodes encoded image bytes through `upload_image_texture_bytes_with_metadata` and releases frame host-cleanup / orphaned resident decoded textures through the sink release hook. Package-scoped texture requests must resolve through mounted bundle candidates from owner/required package provenance; if no mounted bundle matches those package candidates, texture sync must report a missing asset and must not fall back to an unscoped host asset read. The helper `render_frame_with_host_texture_sync` must prepare the frame, release `NativeRendererFrameUpdate.host_cleanup` texture / decoded-image records before submitting the current frame, submit once, upload pending texture assets, release orphaned resident texture resources, and resubmit the same prepared frame only when uploads succeeded so backend draw resource state can see resident textures in that frame cycle; if only cleanup/release happened, the helper must refresh or preserve a post-cleanup texture sync snapshot without resubmitting. `render_json_frame_with_host_texture_sync` must parse and validate `NativeRendererJsonFrameInput` through the Rust JSON facade before calling the texture sync path, so unsafe resolved asset refs fail before host reads. The real-wgpu runtime may copy a completed offscreen frame target to an external `COPY_DST` texture through `copy_frame_to_texture`; this is only a swapchain-present handoff primitive, not a complete window/surface loop. This bridge must preserve package provenance in upload metadata, reject unsafe asset/package references before host reads, report upload/read/release failures, avoid arbitrary filesystem/URL loading, and remain separate from the default Null-backend smoke path. When `QUA_NATIVE_RENDERER_SMOKE_BUDGET` points to a JSON budget file, startup should fail if any smoke metric exceeds its configured max, including frame/backend command complexity, declarative resource counts, resource package/kind counts, fallback-by-package counts, texture upload request/sync counts, total memory, resource-kind memory, package owned/dependent memory, declarative package memory, audio package memory, and audio backend applied plan/command/active-track counts. CLI smoke tests must cover both passing and failing texture sync, frame/backend/declarative/audio scalar budgets, fallback package count, and kind/package/declarative/audio package budget maps. This hook and budget gate must not load QUI/QSS authoring files, ordinary plugins, Web/Cocos/native target core bootstrap arrays, Runtime QPK executable dependencies, or any native payloads.
- `quajs_native_app::texture_sync` owns app-level package release / renderer clear texture cleanup helpers. `release_package_resources_with_host_texture_cleanup` / `clear_renderer_with_host_texture_cleanup` and their media-teardown variants must call the renderer release/clear API first, then route returned `NativeRendererHostCleanupRecord` entries through `sync_texture_releases_from_host_cleanup`. The sync helper may call the upload sink release hook only for `Texture` and `DecodedImage` records, must report already-missing texture handles as non-fatal missing entries, must record release failures, and must ignore declarative/non-texture records such as `UiAst`, `QssStyle`, and `TokenTable` so package release / clear cleanup cannot confuse QUI/QSS ledger resources with GPU textures. Blocked package release must leave resident texture handles untouched.
- `quajs_native_app::texture_sync` also owns an app-level mounted bundle lifecycle registry for texture resources. `NativeTextureBundleMountRegistry` records the last host-observed runtime package bundle set from `NativeHostApi::list_mounted_bundles`, and `sync_mounted_texture_bundle_lifecycle_from_host` compares it with the current host set before a product frame loop or app lifecycle tick. First sync only establishes a baseline; later host unmounts call renderer package release plus host texture cleanup for removed packages. Packages blocked by active renderer unload guards remain tracked so later lifecycle syncs retry instead of silently dropping resident texture handles. `render_frame_with_host_texture_lifecycle_sync` is the low-level product-loop convenience path: it runs resolved projection frame texture sync first, then applies mounted-bundle lifecycle cleanup so stale frame references from the previous projection do not falsely block package texture cleanup. Product loops with active native media backends should use the `*_and_media_teardown` lifecycle variants so package-scoped audio/video teardown succeeds before package texture handles are released; media teardown failures must preserve registry tracking, renderer resources, backend tracks/streams, and resident texture handles for retry. This is transient app/host resource management only; it does not make Rust authoritative for runtime package state or bypass `RuntimeContentManager`.
- `quajs_native_app::product_loop::NativeProductLoop` is the thin Rust-owned projection-frame coordinator for product/native-window paths. It owns only transient native app resources: the texture bundle lifecycle registry and a successful rendered-frame counter. `render_projection_json_with_media_teardown` must parse/validate an already-resolved `NativeRendererJsonFrameInput`, prepare/render the frame, apply native audio/video backend plans, perform frame host-cleanup texture releases, sync pending package-scoped texture uploads, resubmit after successful uploads, run mounted-bundle lifecycle cleanup with media teardown, and increment `rendered_frame_count` only after the full projection/lifecycle pass succeeds. `tick_host_lifecycle_with_media_teardown` is the no-new-frame product lifecycle hook: it observes mounted host bundles, runs package-scoped media teardown before texture cleanup for removed packages, and must not increment `rendered_frame_count` or submit a renderer frame. `shutdown_with_media_teardown` is the product/app teardown hook: it must apply full media teardown before renderer clear, route returned texture host cleanup through the upload sink release hook, reset the mounted-bundle registry only after successful renderer cleanup, and preserve renderer resources/backend tracks/streams/resident handles plus registry tracking on failure so teardown can be retried. `quajs_native_app::product_runtime::NativeProductRuntime` is the reusable product/native-window coordinator that owns the transient renderer, host adapter, and `NativeProductLoop` together so frame rendering, host bundle lifecycle ticks, intent-capable renderer/host access, and shutdown ordering share one product path instead of being reassembled inside smoke/debug code. `quajs_native_app::product_input::NativeProductInputController` is the reusable product-level native input coordinator: it owns only transient cursor position, pointer identity, and IME composition metadata, converts host window coordinates to logical client points, dispatches pointer events through the Rust renderer and `NativeHostApi`, emits focus/blur and default keyboard input-command intents through the existing native host bridge, summarizes IME events by kind plus byte length without retaining raw committed or preedit text, and cancels transient IME composition on window deactivation by emitting the standard `user/text_input` disabled intent once. `quajs_native_app::product_frame_scheduler::NativeProductFrameScheduler` is the reusable transient redraw/present policy helper for product/native-window paths; it owns only target frame count, present attempt count, occluded-final-report policy, grouped present failure metrics, surface recovery metrics, and device recovery metrics. `quajs_native_app::product_window::NativeProductWindowRuntime` is the reusable native-window real-wgpu surface owner for product/native-window paths; it owns only the transient `wgpu::Instance`, `wgpu::Surface`, real-wgpu product runtime, adapter/backend configuration, presentation metadata, resize/present helpers, structured transient present-failure classification for `occluded`, `timeout`, `lost`, `outdated`, `device-lost`, `out-of-memory`, and `fatal` outcomes, and a device-lost recovery hook that rebuilds the real-wgpu target/backend from the signed app's instance/surface while preserving renderer state, media backends, host adapter, and product-loop counters. `quajs_native_app::product_window_loop::NativeProductWindowLoop` is the reusable product-level native window loop coordinator: it combines the window runtime, frame scheduler, resize/device recovery metrics, structured present failure recovery reports, final-frame shutdown ordering, and a completed scheduled-frame counter, while callers still own concrete OS event ingestion and fixture/application frame production. `quajs_native_app::product_app_shell::NativeProductAppShell` combines `NativeProductAppLoop` and a product window loop driver so continuous app shells have one reusable place for lifecycle ticks, redraw scheduling, frame-completion scheduling, and retryable redraw failure recovery; concrete shells still own OS event ingestion, frame source production, request-redraw dispatch, and smoke/product metrics. These paths must preserve blocked package tracking for retry. They must not own scene/progression/runtime package authority, derive game state, load loose resources, persist UI/input browsing state, or become a second bridge; engine/store/runtime package ownership stays in QuaEngine and communication stays through `NativeHostApi` / pipeline-facing contracts.
- `NativeProductAppLoop` must treat retryable redraw failures during hidden/suspended intervals as lifecycle-tick progress rather than terminal loop failure. It may keep transient redraw/lifecycle counters only; it must not own scene progress, package lifecycle authority, or input state.
- Native window smoke IME metrics must report enabled, disabled, preedit, and commit event counts plus last text byte length without retaining raw IME text.
- Rust `quajs_native_app` also has a feature-gated `native-window` smoke path. With `--features native-window` and `QUA_NATIVE_RENDERER_WINDOW_SMOKE=1`, it enables image decoding, creates a `winit` window, creates/configures a real `wgpu::Surface`, bootstraps `RealWgpuNativeRenderRuntimeTarget` through `create_real_wgpu_surface_target`, renders an already-resolved `NativeRendererJsonFrameInput` through `NativeProductRuntime` plus a real `WgpuNativeRenderBackend`, applies frame host cleanup release, decodes/uploads fixture textures from an in-memory `NativeHostApi` into the real WGPU backend, resubmits the prepared frame after successful uploads, then runs mounted-bundle lifecycle sync through the same product-runtime ordering. It handles basic `winit` resize/scale-factor events by reconfiguring the surface and resizing the backend target, converts `winit` cursor/mouse input into native pointer events, cancels transient pointer presses on cursor-leave or focus-loss, cancels transient IME composition on focus-loss or occlusion by emitting one standard disabled text-input intent through the runtime-owned host, records focus/keyboard/IME event counts without persisting raw typed text, emits focus/blur and default keyboard input-command intents through the runtime-owned in-memory `NativeHostApi`, and runs a deterministic pointer probe against the default resolved UI fixture so the Rust renderer can emit a `NativeRendererIntent` through the existing native host bridge. It recovers `Lost` / `Outdated` present acquisition by reconfiguring the current surface size, recovers `DeviceLost` present failures by rebuilding the real-wgpu target/backend from the existing instance/surface, refreshes presented `Suboptimal` surfaces against the current configured size, attempts to acquire/present the surface, and by default exits after one successful scheduled redraw frame. `QUA_NATIVE_RENDERER_WINDOW_SMOKE_FRAMES=<n>` runs up to 120 scheduled redraw frames before exit so CI can prove repeated frame preparation, texture sync, backend submission, surface/device recovery policy, and surface presentation without making the renderer own product state. On the final successful smoke frame, it must call `NativeProductRuntime::shutdown_with_media_teardown`, so the smoke also verifies product-loop media teardown, renderer clear, texture host cleanup, and mounted-bundle registry reset before reporting. It emits `Qua native window smoke json: ...` with adapter, surface format, present mode, `presented`, `presentStatus`, attempt count, `targetFrameCount`, `renderedFrameCount`, resize count/last resize dimensions, `surfaceRecoveryCount`, `surfaceRecoveryAttemptCount`, `surfaceRecoverySuccessCount`, `surfaceRecoveryMissingSizeCount`, `surfaceRecoveryErrorCount`, `deviceRecoveryCount`, `deviceRecoveryAttemptCount`, `deviceRecoverySuccessCount`, `deviceRecoveryMissingSizeCount`, `deviceRecoveryErrorCount`, `presentFailureCount`, `recoverableSurfaceFailureCount`, `recoverableDeviceFailureCount`, `lastPresentFailureKind`, `lastSurfacePresentFailureKind`, `lastRecoveryAction`, `lastSurfaceRecoveryStatus`, `lastDeviceRecoveryStatus`, texture upload counts, texture lifecycle sync/baseline/tracked/blocked/release/cleanup-error counts, texture shutdown cleanup counts, pointer event/dispatch/intent/probe/cancel counts plus the last emitted intent type, focus gain/loss counts plus focus intent emit count, keyboard press/release/repeat counts plus keyboard intent emit count, IME event/preedit/commit counts plus composition-active, last cursor range, and last committed/preedit byte length, dimensions, pass/command counts, and submitted command-buffer count; texture upload error counts include host read/upload failures, orphan release failures, and frame host cleanup release failures, while texture shutdown cleanup errors are reported separately. `presented=false` with `OccludedAfterRetry` means window/surface/offscreen rendering ran but the OS never exposed an acquirable visible swapchain texture, so do not claim swapchain presentation was verified in that environment. This smoke must not load Web/Cocos/native target core bootstrap arrays, ordinary plugins, Runtime QPK executable dependencies, filesystem/network assets, or native payloads; fixture texture bytes remain in-memory host assets only.
- Rust `quajs_wgpu_renderer` may bootstrap an externally owned real-wgpu surface into a `RealWgpuNativeRenderRuntimeTarget` through `create_real_wgpu_surface_target`, including adapter selection, device/queue creation, native surface config planning, and `surface.configure`. It may also plan and apply real-wgpu surface configuration behind the `real-wgpu` feature through `configure_wgpu_surface_for_native_renderer`, requiring swapchain textures to support `RENDER_ATTACHMENT | COPY_DST` so `copy_frame_to_texture`, `present_frame_to_surface_texture`, and `present_frame_to_surface` can hand off the offscreen frame to a swapchain texture. Real-wgpu runtime devices may resize their target from a new surface configuration through `resize_target_from_surface_config`; resize must rebuild frame uniforms and the offscreen frame target while preserving reusable GPU resources, and must clear cached pipelines/bind groups when the surface color format changes. The native app now has a reusable projection-frame product runtime, a reusable product input coordinator for pointer/focus/keyboard/IME bridge behavior, a reusable product frame scheduler, a reusable product window runtime for real-wgpu surface ownership/presentation/resize/device rebuild, a reusable product window loop coordinator for redraw/present/recovery/final shutdown policy, a reusable product app shell coordinator for continuous lifecycle/redraw/retry policy, and a feature-gated scheduled-redraw window smoke that consumes those coordinators while keeping only winit event handling, fixture frame production, input metrics, and pointer probes locally. The smoke preserves structured present-failure labels for occluded, timeout, lost, outdated, validation, device-lost, out-of-memory, and fatal outcomes; covers basic resize reconfiguration, `Lost` / `Outdated` present recovery, `DeviceLost` backend rebuild, presented `Suboptimal` surface refresh against the current configured size, repeated frame rendering when requested by `QUA_NATIVE_RENDERER_WINDOW_SMOKE_FRAMES`, pointer intent dispatch through the runtime-owned `NativeHostApi`, focus-loss/occlusion IME composition cancellation through `user/text_input`, and app-level mounted bundle lifecycle cleanup helpers for texture resources. Loop-level device-lost recovery fixtures cover success, missing-size, and rebuild-error paths without a real GPU; richer product text-input/IME composition UX and platform-specific real device-loss fixtures remain separate work.
- Real WGPU frame capture reads the most recently completed offscreen frame target through a padded `COPY_SRC` texture-to-buffer transfer, converts BGRA targets to RGBA8, and may encode the result as PNG behind `image-decode`. `NativeProductWindowRuntime::capture_frame_png` is a transient product-shell API only: it must run after a completed render pass and before final renderer shutdown. `@quajs/engine-native` responds to the standard `save_preview/capture_request` pipeline event only when the product shell explicitly injects `captureSavePreview`; it returns the provider's actual bytes, MIME type, dimensions, and capture timestamp through `save_preview/capture_result`, or a recoverable `save_preview/capture_error` when the provider is absent or fails. Window smoke captures its final presented frame before shutdown and reports `frameCaptureMimeType`, byte count, dimensions, PNG signature validity, visible-pixel count, and colored-pixel count; `demo` command `native:smoke:save-preview` must reject missing/invalid PNG bytes, empty/black frames, or dimensions that differ from the physical WGPU target. Null/headless bridges must not fake pixels or advertise a save-preview capability, and native PNG capture must not claim WebP/JPEG, resize, overlay filtering, or background-policy support unless the injected provider actually implements and tests those semantics.

## Package Responsibilities

- `@quajs/native-contracts`: serializable native host, renderer capability, compatibility, QUI/QSS, and target bootstrap contracts. Packagers should use `createTargetCoreSelection` to derive the active Web/Cocos/native resolver identity, core family, and selected adapters from one target value; native packagers should use `createNativeCapabilityManifestPayload` / `createNativeCapabilityManifestHash` with an injected SHA-256 implementation when emitting `nativeRenderer.capabilityManifestHash`; use `createTargetBundleNativeRendererInfo` to derive native target-bundle renderer metadata from the actual renderer capabilities. Keep `@quajs/native-contracts` platform-neutral and covered by source-level import isolation tests so shared validation code does not import Web, Cocos, or native runtime adapter packages.
- `@quajs/engine-native`: engine plugin/adapter that reads native host info, registers renderer capabilities, supplies runtime package compatibility guards, and exposes the restricted native `RuntimeModuleLoader` over package asset bytes plus a Rust/QuickJS evaluator.
- `@quajs/engine-native` owns the stateless native renderer JSON frame serializer from engine-owned `QuaViewProjection`-compatible data to Rust `NativeRendererJsonFrameInput`, including dialogue/choices/UI shape normalization, explicit required visibility/mode/enabled fields, and package provenance preservation. It must not hold renderer state or reinterpret engine progression.
- `@quajs/engine-native` also owns the native renderer intent bridge. It installs a host-side `emitRendererIntent` callback during plugin initialization, parses Rust `NativeRendererIntent` payloads, emits `choice/select` as `RenderToLogicEvents.USER_CHOICE_SELECT`, emits `ui/intent` as a generic render-to-logic UI intent, forwards `open` / `close` / `update` actions to the existing UI overlay request events, maps native `user/input_command`, `window/focus`, and `window/blur` intents to the existing render-core render-to-logic events, and forwards standard audio renderer events (`audio/ended`, `audio/interrupted`, `audio/unlocked`, and `audio/error`) into the existing audio pipeline event names without importing `@quajs/plugin-audio` or creating a second event bus.
- Native host bridges may expose `drainRendererIntents` only as a transient host ledger drain for TS-side pipeline forwarding after Rust renderer/input/audio helpers emit `NativeRendererIntent`; use `drainNativeRendererIntentsToPipeline` or `NativeHostPlugin.drainRendererIntents()` to forward drained events into the existing pipeline, and do not treat drained intents as authoritative renderer state or a parallel native event bus.
- `@quajs/assets-native`: QuaAssets adapter over native host byte/storage/crypto APIs.
- Native asset hosts should provide `listStorageKeys` when cache roots need full cleanup; `@quajs/assets-native` may fall back to index-known asset deletion when key listing is unavailable, but full orphan cleanup requires host prefix listing. Native asset cache roots are host storage prefixes and must be safe package-relative namespace paths; leading/trailing `/` may be normalized away, but empty segments, `.`, `..`, backslashes, URLs, and URI schemes are invalid.
- `@quajs/store-native`: QuaStore persistence adapter over native host storage APIs.
- Native store hosts must provide `listStorageKeys` when using list or prefix-clear save operations; missing key listing support must fail explicitly instead of making snapshots/save slots appear empty.
- `@quajs/native-ui-compiler`: native QUI/QSS authoring compiler for parse, validation, formatting, completions, hover metadata, component registry, QSS feature registry, static QUI/QSS surface projection compilation, projection requirement collection via `collectNativeUiSurfaceProjectionRequirements`, and native UI surface compatibility metadata via `createNativeUiSurfaceCompatibilityFromDocuments` / `createNativeUiSurfaceCompatibilityFromProjection`. Projection requirement collection must distinguish resolved projection fields (`visible`, `opacity`, `scrollOffsetX`, `scrollOffsetY`, `provenance`, etc.) from QSS feature names so runtime compatibility metadata does not confuse compiler-only projection shape with QSS declaration support. It may depend on platform-neutral contracts only and must not import Web, Cocos, or native runtime bootstrap adapters.
- `@quajs/native-language-server`: standalone `.qui/.qss` LSP adapter, in-memory project index, document-link helpers, component/class/id definition, reference, and rename edit queries, QUI/QSS package-relative asset reference indexing for `Image(src|image)` and QSS `asset(...)`, LSP definition/documentLink/references/rename providers, missing asset diagnostics, asset quick fixes, and a package bin that defaults to stdio when no transport argument is supplied. The pure project index should only create candidate links; the LSP server may resolve existing `file://` candidates with a read-only file-exists check for unopened resources and emit `NATIVE_UI_ASSET_MISSING` warnings for unresolved asset candidates. Asset quick fixes may remove missing/invalid resource references through `quickfix` / `source.fixAll.quaNativeAssets`, but must not create files, scan workspaces, load resources, or trigger target runtime/bootstrap code. It is an authoring tool only; it must not load Web/Cocos/native target core bootstrap plugins, ordinary game plugin arrays, or runtime QPK executable dependencies. Keep source import and `package.json` dependency isolation tests so LSP tooling cannot depend on Web, Cocos, or native runtime/bootstrap packages.
- `packages/native/vscode` (`qua-native-authoring` VSCode extension): standalone editor extension for native `.qui/.qss` files. It should start the native LSP, contribute language ids `qua-ui` and `qua-style`, expose commands for format, validation, restart, and applying the LSP `source.fixAll.quaNativeAssets` action to the active native UI document, and avoid importing or depending on `@quajs/renderer-web`, `@quajs/renderer-cocos`, native app/runtime bootstrap packages, or other target core adapters. Keep source import and `package.json` dependency isolation tests in this package so extension tooling cannot accidentally pull Web, Cocos, or native runtime/bootstrap packages into the authoring path.
- `@quajs/native-benchmarks`: deterministic native authoring/tooling smoke benchmarks. It measures QUI/QSS parse+validate, QSS style resolution, QSS format idempotence plus resolved-style IR stability (`declarations`, `formatIdempotentPasses`, `formattedBytes`, `resolvedRules`, and `styleFields`), static surface projection compilation, resolved-projection requirement collection, projection-derived native UI surface compatibility metadata, format, completion, hover, native asset code action/fixAll generation, project index build/incremental update, document-link/reference counts, document size, diagnostics, elapsed time, and process memory deltas through `@quajs/native-ui-compiler` and `@quajs/native-language-server` public APIs only. It must not start a renderer, load Web/Cocos/native target core bootstrap adapters, parse ordinary game plugin arrays, access the network, or use randomized fixtures. Keep source import and `package.json` dependency isolation tests in this package so benchmark and acceptance tooling cannot accidentally depend on Web, Cocos, or native bootstrap core packages.
- Rust `quajs_native_runtime`: QuickJS host and native host API implementation.
- Rust `quajs_wgpu_renderer`: wgpu projection renderer and transient resource management.

## Target Isolation

Web, Cocos, and native target core adapters must not be mixed:

- Web uses Web assets/renderer/framework adapters only.
- Cocos uses Cocos host/renderer only.
- Native uses `@quajs/engine-native`, `@quajs/assets-native`, `@quajs/store-native`, and Rust native runtime/renderer metadata only.
- Web, Cocos, and native core bootstrap plugins are target roots, not ordinary game plugins. They must not be placed in one shared plugin array, umbrella preset, Runtime QPK executable dependency list, or runtime resolver path that imports every target and chooses one later.
- Web, Cocos, and native packaging must use separate target-core resolver contexts. Shared validation schemas are fine, but a shared all-target core plugin list is invalid even if later filtering appears to remove inactive entries.
- Packaging to Web, Cocos, or native must begin from the active target resolver. Do not build `[webCore, cocosCore, nativeCore]` or any shared all-target bootstrap preset and filter afterward; inactive target core plugins must not be imported, registered, bundled, or kept as executable QPK dependencies.
- `@quajs/native-contracts` may be used by Web/Cocos build tooling for target validation, but it must not remain in Web/Cocos runtime bundles after tree-shaking.
- Target selection must happen before bootstrap/plugin resolution. Do not build an umbrella app that imports Web, Cocos, and native core plugins and chooses one at runtime.
- Core target plugins are reserved bootstrap infrastructure, not normal game plugins. Do not put Web/Cocos/native bootstrap adapters in ordinary plugin arrays, shared presets, Runtime QPK executable dependencies, or generated plugin resolver paths.
- Quack/native packaging should materialize one read-only `TargetCoreSelection` for `web`, `cocos`, or `native` before resolving normal engine/game plugins. Shared plugin resolution may consume that selection but must not add another target core family. The emitted `target-bundle-manifest.json` must record the matching resolver identity as `targetCoreResolver`: `web-core-resolver`, `cocos-core-resolver`, or `native-core-resolver`.
- Packaging a Web, Cocos, or native project must select exactly one target bootstrap. Target-specific renderer plugin entries and core adapters are not interchangeable between targets.
- Web, Cocos, and native project templates/starters are target-specific shells. Web starters may consume only the Web target-core resolver, Cocos starters only the Cocos target-core resolver, and native starters only the native target-core resolver; debug shells, smoke runners, installers, and updaters must consume the emitted active-target `target-bundle-manifest.json` rather than importing or re-declaring any target core adapter.
- When generating Cocos, Web, or native projects, target core plugin wiring must happen only in that target's packager resolver. Project templates, generated startup code, debug/release shells, installers, updaters, and smoke runners must never carry another target's core plugin and filter it later.
- Packaging to Cocos, Web, and native projects must keep target core plugins out of ordinary project templates entirely. A template, starter, debug shell, installer, updater, smoke runner, or runtime package that imports multiple core families and then filters by target is a release-blocking target-core leak even when the final emitted manifest appears single-target.
- Treat the three target bootstrap plugin families as disjoint: `web-core`, `cocos-core`, and `native-core`. Every packaged artifact must record exactly one selected family, and `target`, selected core adapters, renderer entries, and Runtime QPK executable dependencies must all agree with that family.
- Treat target core plugins as release-blocking isolation boundaries. Web artifacts must exclude Cocos/native core adapters, Cocos artifacts must exclude Web/native core adapters, and native artifacts must exclude Web/Cocos core adapters after bundling/tree-shaking, not just in source metadata.
- Shared engine/game/plugin code may be reused across targets only through platform-neutral entries. Shared entries must not eagerly import `@quajs/renderer-web`, `@quajs/renderer-cocos`, `@quajs/engine-native`, `@quajs/assets-native`, `@quajs/store-native`, Cocos host adapters, or native runtime adapters.
- Validate target isolation at three layers: application bootstrap core adapters, target-specific renderer/plugin entries, and Runtime QPK renderer compatibility metadata. Do not let a pass in one layer imply the others are safe.
- Use `validateExclusiveTargetBootstrap` from `@quajs/native-contracts` to assert a packaged app/startup dependency set registers exactly one Web, Cocos, or native bootstrap. Then use `validateTargetBootstrap` for the active target and treat both `missing` required target adapters and `forbidden` cross-target adapters as blockers.
- Use `validateOrdinaryPluginListTargetIsolation` from `@quajs/native-contracts` before ordinary game/plugin resolution. Quack packagers should call `assertQuackPluginReferencesTargetIsolation` / `assertLoadedQuackPluginTargetIsolation` at CLI, config, generated resolver, and loaded-plugin boundaries. Any Web, Cocos, or native target core root or subentry in `plugins`, shared presets, or generated plugin resolver paths is a blocker; target bootstrap must come only from the active target-core resolver. When ordinary plugin references include both `specifier` and `packageName`, both fields must be normalized and checked so a safe-looking package name cannot hide a target core subentry in the specifier, or vice versa.
- Use `validateTargetPluginManifest` from `@quajs/native-contracts` for third-party and official plugin source metadata before bundle entry selection. Plugin entry `target` metadata must be one of `shared`, `web`, `cocos`, or `native`; shared plugin entries must import no target core adapters, selected entries must match the active target, inactive target entries must not be eager, and target entries may import only their own target adapters plus shared logic.
- Inactive eager plugin entries must also report any Web/Cocos/native core adapter imports they would pull into the active artifact, so packagers can reject and locate target-core leakage before bundling.
- Use `validateTargetBundleManifest` from `@quajs/native-contracts` for emitted `target-bundle-manifest.json` files. Pass `expectedTarget` from the packaging/runtime context so a manifest declared for Web, Cocos, or native cannot be silently reused by another target. When `expectedTarget` is present, resolver, selected core plugin family, selected core adapters, renderer entry targets, runtime package target-core guards, artifact metadata, and native renderer metadata must all be checked against that packaging/runtime context target rather than trusting the manifest's self-declared target. It validates `targetCoreResolver`, the explicit selected core adapter set, normalized runtime dependencies, selected renderer entry targets, Runtime QPK renderer entry targets, Runtime QPK executable dependencies, `projectGraphs`, and target-scoped native renderer metadata together after bundling/tree-shaking.
- Emitted target bundle manifests may use `projectGraphs` to record project-template, startup-shell, debug-shell, release-shell, smoke-runner, installer, updater, dev-server, post-bundle, or custom dependency graphs. Non-`post-bundle` graphs must remain platform-neutral and must not declare any Web/Cocos/native target core adapter, including the active target family; `post-bundle` graphs may contain the active core family but must still reject inactive target core roots or subentries, including residual adapters from a Web/Cocos/native all-target union that was filtered too late. This lets templates and shells prove they consume the active target manifest instead of re-declaring bootstrap plugins.
- Native target bundle validation must reject missing, empty, or invalid artifact metadata required by packaging and startup, including `profile` (`debug`/`release`) and native `platform` (`macos`/`windows`/`linux`).
- Native target bundle validation must reject missing or empty release app metadata required by packaging and startup, including `app.bundleId`, `app.version`, `app.buildNumber`, and `app.icon`.
- Native target bundle validation must reject missing, non-string, or whitespace-only native renderer identity/capability metadata. `nativeRenderer.version`, `nativeRenderer.capabilityManifestHash`, and every `nativeRenderer.capabilityIds[]` value must be real non-empty strings so release artifacts cannot pass startup gates with placeholder renderer metadata.
- Native project packaging metadata should come from the unified Qua project manifest `targets.native`, including `platforms`, `profiles`, `outputDir`, `app.bundleId`, `app.version`, `app.buildNumber`, `app.icon`, native asset target, and build options.
- Quack native packaging should consume `createQuaProjectNativeArtifactPlans` so debug/release outputs are isolated by `outputDir/profile/version-buildNumber/platform` before target-bundle manifests, signing, or distribution artifacts are written.
- Quack packagers should use `emitQuaTargetBundleManifest` to validate and write Web, Cocos, and native `target-bundle-manifest.json` files with the active `expectedTarget`. Native packaging should use `emitQuaProjectNativeTargetBundleManifest` as the native artifact-plan wrapper around the shared emitter.
- Native release target-bundle manifests are immutable within a `versionSegment` + platform artifact directory. `emitQuaProjectNativeTargetBundleManifest` must reject overwriting an existing release `target-bundle-manifest.json` when the new manifest differs; debug manifests may be regenerated in place for local iteration.
- Quack/native packaging must emit a post-bundle `target-bundle-manifest.json` for every Web, Cocos, and native debug/release artifact. Native target manifests created by `createQuaProjectNativeTargetBundleManifest` automatically append a `native.<profile>.<platform>.post-bundle` `projectGraphs` record from dependencies, renderer entries, and Runtime QPK executable/renderer references; callers may pass additional template/startup/installer/updater/smoke graphs, which must stay platform-neutral. Validate the emitted manifest after tree-shaking, not just the source dependency set.
- Runtime startup must repeat the exclusive-target assertion before engine initialization so hand-built shells cannot register zero or multiple target core adapter sets. Native startup should use `createNativeEngineBootstrap` and pass the emitted `target-bundle-manifest.json` into its `targetBundleManifest` option when available, falling back to `targetBootstrapPackages` only for lightweight tests or hand-built debug shells. After reading host info, `@quajs/engine-native` must verify emitted app identity/profile/platform metadata plus native renderer package/version/backend/declared backendVersion/capability ids and capability manifest hash against the actual Rust host info before exposing the plugin as initialized.
- Rust `quajs_native_app` startup must validate native `target-bundle-manifest.json` data before building `NativeHostInfo` or starting QuickJS/engine. It can load the emitted manifest from `QUA_NATIVE_TARGET_BUNDLE_MANIFEST` and should reject unreadable/invalid manifest files, invalid artifact `profile`/`platform` values, mismatched or missing `targetCoreResolver`, missing or empty app metadata (`app.bundleId`, `app.version`, `app.buildNumber`, `app.icon`), app identity/profile/platform mismatches, missing or invalid `nativeRenderer` package/version/backend/capability metadata, missing or empty `nativeRuntime.quickjsVersion` / `nativeRuntime.nativeRuntimeVersion` / `nativeRuntime.assetAdapterVersion` / `nativeRuntime.storeAdapterVersion`, foreign Web/Cocos core roots, non-native renderer entry targets, Runtime QPK target-core executable/renderer entries, and `projectGraphs` that either declare any target core outside `post-bundle` or include inactive Web/Cocos core adapters in `post-bundle` before host-info construction. Startup dependency normalization must handle bare package specifiers, `?query` / `#hash` bundler suffixes, Windows/backslash paths, `node_modules` paths, and pnpm `.pnpm` store paths before deciding whether a dependency is Web/Cocos/native core. After constructing host info, it must compare emitted renderer package/version/backend/declared backendVersion/capability ids, capability manifest hash, QuickJS version, native runtime version, asset adapter version, and store adapter version against the actual Rust host info before QuickJS or engine startup.
- Validate package subentries by normalized package root; for example `@quajs/renderer-web/plugins/audio` is still Web core, and must not appear in native/Cocos bundles.
- Validate both `specifier` and `packageName` fields on target bundle references, plugin import references, and Runtime QPK executable/renderer references. Normalize bare subentries, `npm:` specifiers, `?query` / `#hash` suffixes, Windows/backslash paths, `node_modules` paths, and pnpm `.pnpm` store paths to package roots before target-core classification. A safe-looking `packageName` must not mask a Web, Cocos, or native target-core subentry/path in `specifier`, or vice versa.
- Shared plugin entries must not import Web, Cocos, or native core adapters. Target entries may import only their own target adapters plus platform-neutral shared logic.
- Multi-target plugin packages may declare Web, Cocos, and native entries in source metadata, but packaging must include only the active target entry and reject accidental imports of inactive target entries.
- Renderer entry records in emitted target bundle manifests must explicitly declare their target. A Web/Cocos/native artifact must fail validation if an app renderer entry or Runtime QPK renderer entry omits target metadata or declares another target, even when the entry comes from a third-party plugin package rather than an official core adapter package.
- Runtime QPKs may declare compatibility with Web, Cocos, and native, but inactive target blocks are metadata only. They must not carry executable dependencies on target core adapters, install target bootstrap plugins, or activate another target bootstrap.
- Runtime package guard must reject `executableDependencies` and `rendererEntries` that normalize to any Web, Cocos, or native target core adapter root, including target subentries such as `@quajs/renderer-web/plugins/audio`, `@quajs/renderer-cocos/plugins/dialogue`, or `@quajs/engine-native/runtime`.
- Dynamic QPK activation must evaluate only the active artifact target block. A Web build ignores native/Cocos compatibility blocks, a Cocos build ignores Web/native blocks, and a native build ignores Web/Cocos blocks while still rejecting native-code payloads for native.
- Every packaged debug/release artifact should emit a target bundle manifest and run dependency graph checks so Web builds exclude Cocos/native core adapters, Cocos builds exclude Web/native core adapters, and native builds exclude Web/Cocos core adapters.
- Runtime startup should assert exactly one target adapter set registered with the engine, so hand-built bundles cannot mix Web, Cocos, and native core plugins.
- Implement target core plugin resolution in this order: normalize target, select exactly one bootstrap preset, materialize that target's adapters/renderer/host bridge, resolve shared engine/game plugins, select active third-party target entries, bundle/tree-shake, emit `target-bundle-manifest.json`, validate it, then assert it again at runtime startup.
- Implement target core plugin resolution through target-specific functions or contexts such as Web-only, Cocos-only, and native-only resolvers. Do not let debug/release/updater/installer paths bypass the same emitted manifest validation.
- Do not introduce all-target core helpers such as `resolveAllTargetCore()`, `createCorePluginsForAllTargets()`, umbrella target core presets, or barrel exports that import Web, Cocos, and native bootstrap adapters before filtering. Target-specific resolvers may share schema and metadata helpers only when those helpers do not import inactive target bootstrap entries.
- Treat target isolation CI as two mandatory layers: a contracts suite with symmetric Web/Cocos/native fixtures for bootstrap, ordinary plugin, plugin manifest, and target bundle validation; and a packager suite that proves debug, release, installer, updater, hand-built shell, and post-bundle dependency graph paths all call the same isolation helpers. Include a negative fixture where code first constructs the Web/Cocos/native core plugin union and filters afterward; it must fail even if the final manifest appears single-target.
- Contracts target-bundle isolation tests must include both non-`post-bundle` all-target core union fixtures and `post-bundle` fixtures where the active target core is present alongside inactive Web/Cocos/native core adapters; only the active family may survive in `post-bundle`.
- Run the same source selection, renderer entry, post-bundle graph, runtime startup, and Runtime QPK gates for all three targets. Native cannot be the only strict path; Web and Cocos builds must reject native core adapters with the same severity that native builds reject Web/Cocos adapters.
- Test target isolation symmetrically. Web fixtures must reject Cocos/native leakage, Cocos fixtures must reject Web/native leakage, and native fixtures must reject Web/Cocos leakage. Native-only rejection tests are not enough.
- When packaging Web, Cocos, or Native projects, treat core bootstrap plugins as mutually exclusive roots at every layer: bootstrap selection, ordinary plugin lists, shared presets, third-party target entries, renderer entries, Runtime QPK executable dependencies, post-bundle dependency graphs, emitted manifests, installer/updater artifacts, and runtime startup. A dependency hidden in either `specifier` or `packageName` is still leakage.

Shared engine/game/plugin packages may be reused only when platform-neutral.

## QUI/QSS Projection

- Inline QUI surface nodes are renderer projections, not authoritative UI state.
- Supported foundational QUI node kinds are `Fragment`, `Box`, `Stack`, `Row`, `Column`, `Grid`, `Backdrop`, `Button`, `Divider`, `Layer`, `SafeArea`, `Spacer`, `Text`, `RichText`, `Image`, `Panel`, and `Scroll`; higher-level components such as dialog, drawer, save/load panels, gallery, and settings should compose from these base nodes.
- Imported or package-local composite QUI components are authoring-time structures only. `Dialog`, `Drawer`, save/load panels, gallery, settings, and third-party composites must be expanded or flattened by TS compiler/tooling into foundational `NativeUiSurfaceNodeProjection.kind` values before Rust receives resolved JSON; do not add arbitrary composite names to Rust surface DTOs or native-wgpu capability manifests unless they become official primitive capabilities. `@quajs/native-ui-compiler` may expand project/package composites through explicit `compileNativeUiSurfaceProjection({ components })` document maps supplied by tooling or packagers; it must not load component files from disk, QPKs, URLs, or runtime host APIs by itself.
- QUI authoring supports declarative conditional and loop directives in compiler/tooling only. `else-if` / `else` branches must be adjacent to an active conditional chain, duplicate directives on one node are invalid, `for` supports `item in source` and `(item, index) in source`, and loop-rendered nodes must declare a stable `key` so native renderers never infer list identity.
- Native QUI template expressions are evaluated only by the TS compiler's restricted evaluator. The P0 subset includes `props.*`, `view.*`, `settings.*`, loop bindings, literals, object/array literals, `== != < <= > >= && || ! ??`, and ternary for conditions, loop sources, text/prop binding, composite props, and action descriptor arguments. Do not use `eval`, `Function`, QuickJS, Rust-side parsing, arbitrary calls, assignment, mutation, `new`, or `await` for QUI expression handling.
- `@quajs/native-ui-compiler` must normalize QUI `action:` values through shared action descriptors before projection. `ui.*` emits `ui/intent` with the method as `action`, `choice.select(choiceId)` emits `choice/select`, and `save.*` / `settings.*` stay declarative `ui/intent` actions with namespaced action names. Native renderer capability manifests must declare only the actual renderer event envelopes (`ui/intent` and `choice/select` for UI surfaces), not namespaced action names such as `save/select` or `settings/change`. Do not re-parse action strings in LSP, projection compiler, or Rust renderer paths.
- Native-wgpu surface intents are currently projectable only on `Button`, `Box`, `Backdrop`, and `Panel`. `@quajs/native-ui-compiler` and the native LSP must diagnose `action:` on registered foundational components that do not emit pointer intent surfaces, such as `Image`, `Text`, `RichText`, `Divider`, `Scroll`, `Spacer`, and structural groups, using `QUI_UNSUPPORTED_ACTION_TARGET`. Projection compilation and document/projection-derived compatibility metadata must omit those non-projectable intents even if callers ignore diagnostics, and the Rust JSON facade must reject malformed resolved JSON that still carries an `intent` on a non-projectable node kind instead of silently dropping it. Developers who need clickable images, text, or scroll regions should compose them inside `Button`, `Box`, `Panel`, or `Backdrop`, or use a composite that expands to those primitives.
- Native UI `choice/select` intents must carry canonical `choiceId` on the resolved intent projection, not only inside arbitrary metadata. Rust JSON facade must reject `choice/select` UI surface intents without canonical `choiceId`; native intent serialization must let canonical `choiceId`, `action`, and `elementId` overwrite same-named metadata fields so dynamic UI content cannot forge dispatch identity. Rust JSON facade must also reject unsafe or oversized UI intent metadata before that metadata can become native host `payloadJson`.
- `@quajs/native-ui-compiler` exposes `NativeQuiDocument.tree` and `parseQuiStructureTree` as the shared authoring AST for projection compiler and LSP features; Rust native renderer must not parse QUI or QSS authoring syntax.
- QUI component registry entries must declare a content model (`children`, `text`, or `none`). Compiler/LSP diagnostics should reject nested QUI components inside `Text`/`RichText`, child content inside no-content leaves such as `Image`/`Divider`/`Spacer`, orphaned named slots, unknown parent slots, and duplicate slot blocks before Rust receives a projection tree.
- Rust `quajs_wgpu_renderer` JSON facade must defensively reject malformed resolved projection JSON that carries a present non-array `children` field on any native UI surface node, and non-empty `children` on native QUI leaf/no-content node kinds (`Text`, `RichText`, `Image`, `Divider`, or `Spacer`). This is only a resolved bridge guard; Rust must not parse QUI authoring syntax or perform compiler content-model analysis.
- Native QUI hover metadata should come from the same component registry and expose content model, slots, and style parts instead of duplicating component documentation in the VSCode extension.
- `Fragment` is a command-free structural grouping node. It expands its children in place, inherits parent clip/z context, and must not emit draw commands or pointer intent surfaces.
- `Stack`, `Row`, `Column`, and `Grid` are command-free structural layout group nodes emitted after QUI/QSS layout resolution. They expand children in place and must not parse QSS, own layout state, draw, or emit pointer intent surfaces. If their resolved projection carries `clipChildren`, Rust may add their resolved bounds to the child clip context for drawing and hit testing without turning them into painted nodes.
- `Divider` projects a visual separator only. It must not emit pointer intent surfaces and should be used by composites instead of ad hoc line primitives.
- `Layer` is a command-free structural z-group. It shifts child draw-command z indexes using the resolved node `z_index`, but must not draw, own state, or emit pointer intent surfaces. If its resolved projection carries `clipChildren`, Rust may add its resolved bounds to the child clip context for drawing and hit testing without turning it into a painted node.
- `SafeArea` is a command-free structural clip container. It constrains child draw commands and hit testing to already-resolved logical safe-area bounds without owning state.
- `Spacer` is a command-free layout spacing node emitted after QUI layout resolution. It must not draw, own state, emit pointer intent surfaces, or affect authoritative game state.
- `Box` projects a generic rectangular paint primitive. It is visual-only unless it carries an explicit intent, in which case pointer input may resolve that intent through `@quajs/pipeline`.
- `Backdrop` projects a semantic overlay panel. It is visual-only unless it carries an explicit intent, in which case pointer input may resolve that intent through `@quajs/pipeline`.
- `Panel` projects a semantic container panel. It is visual-only unless it carries an explicit intent, in which case pointer input may resolve that intent through `@quajs/pipeline`.
- `Image` projects a package-backed image leaf only when the resolved projection carries a non-empty safe asset type/name. Missing or invalid authoring `src` / `image` values should remain compiler/LSP diagnostics; Rust may skip an empty resolved image leaf instead of emitting a meaningless empty image draw command.
- `Scroll` currently projects a scroll panel plus clip-start/clip-end commands, per-command clip bounds for hit testing, and already-resolved `scrollOffsetX` / `scrollOffsetY` child projection offsets. Rust may apply those offsets to draw and hit-test bounds, but it must not own persisted scroll position or authoritative UI state.
- Rust `NativeRenderer::pointer_event_and_emit_intent` may emit a resolved pointer intent through a caller-provided `NativeHostApi` as a `NativeRendererIntent` (`type` plus optional `payloadJson`). UI intent metadata from resolved QUI action descriptors must be preserved into that payload; canonical `action`, `choiceId`, and `elementId` fields from renderer intent resolution override same-named metadata so dynamic content cannot forge core dispatch identity. This is only a dispatch helper over the existing native host bridge; it must not own input authority or introduce a second event bus.
- `@quajs/engine-native` must translate Rust-emitted `NativeRendererIntent` values into `@quajs/pipeline` render-to-logic events. `choice/select` requires a string `choiceId`; `ui/intent` preserves generic `{ action, elementId, ...metadata }` payloads and only maps conventional `open` / `close` / `update` actions to overlay request shortcuts; `user/input_command` must validate the existing render-core command/device/source/timestamp payload shape before emitting `RenderToLogicEvents.USER_INPUT_COMMAND`; `window/focus` and `window/blur` emit the existing lifecycle events with empty payloads. Malformed renderer intent payloads must be reported as renderer errors through the existing bridge rather than dispatched through a parallel native event path.
- Native QUI nodes may carry `UiSurfaceResolvedStyle`, representing already-resolved QSS declarations from compiler/runtime tooling.
- Native composite expansion is compiler-only. Imported component documents supplied to `compileNativeUiSurfaceProjection` may use `props.*` bindings plus named slot placeholders such as `slot header` / `slot body`; invocation content replaces those placeholders before projection, loop keys scope expanded child ids, and the resulting projection must contain only foundational native-wgpu node kinds.
- `@quajs/native-ui-compiler` owns QSS declaration normalization through `resolveNativeQssDeclarations` and related resolved-style types. It should emit `NativeQssResolvedStyle` plus node-level `zIndex` metadata from already parsed declarations; Rust/wgpu must consume this resolved shape instead of parsing QSS declarations.
- `@quajs/native-ui-compiler` owns `compileNativeUiSurfaceProjection`, which lowers analyzed `NativeQuiDocument.tree` plus analyzed QSS rules into camelCase `NativeUiSurfaceProjection` / `NativeUiSurfaceNodeProjection` JSON compatible with Rust `UiSurfaceNodeProjection`. When compiling dynamic package surfaces, pass explicit `contentPackageId` / `requiredRuntimePackages` so the emitted node-level `provenance` reaches Rust draw commands, resource requests, memory metrics, and unload blockers. This is a tooling/compiler boundary only; Rust renderers must continue to reject malformed projection JSON rather than loading QUI/QSS source text.
- Native QSS property value completions and hover metadata must come from the shared `@quajs/native-ui-compiler` registry so the compiler, language server, VSCode extension, and benchmark paths do not duplicate supported value lists.
- Native QSS diagnostics for native-wgpu supported properties must reuse the same parser semantics as resolved style generation and reject invalid values before projection with `QSS_INVALID_VALUE`; do not let Rust infer fallback meaning for malformed QSS values. `background-color`, `border-color`, and `color` accept only safe native color literals: hex, comma-form `rgb(...)` / `rgba(...)`, `transparent`, `currentColor`, or basic named colors. Browser `url(...)`, path-like strings, traversal markers, arbitrary CSS color functions, percentages, and malformed numeric channels must be diagnostics before projection.
- Native QUI `src` / `image` resource props must be package-relative string literals, and `asset-type` must be a safe native asset kind. `@quajs/native-ui-compiler` should emit `QUI_INVALID_ASSET_REFERENCE` for URLs, absolute paths, traversal, empty or `.` path segments, trailing slashes, backslashes, native payload-looking suffixes, non-literal asset expressions, or invalid asset kinds, and projection must not pass unsafe image resources to Rust. The compiler, QSS parser, language-server asset index, and native UI compatibility derivation must share the same package asset-name safety helper so invalid resources cannot become document links, resolved projection image refs, or runtime compatibility `assetKinds`.
- Current native resolved QSS projection fields are `align-items`, `background-color`, `background-image`, `background-position`, `background-size`, `border-color`, `border-radius`, `border-style`, `border-width`, `bottom`, `box-sizing`, `color`, `display`, `font-family`, `font-size`, `font-style`, `font-weight`, `gap`, `row-gap`, `column-gap`, `justify-content`, `margin`, `margin-top`, `margin-right`, `margin-bottom`, `margin-left`, `position`, `inset`, `left`, `right`, `top`, `width`, `height`, `min-width`, `max-width`, `min-height`, `max-height`, `letter-spacing`, `line-height`, `text-align`, `text-decoration`, `text-overflow`, `text-transform`, `white-space`, `object-fit`, `object-position`, `opacity`, `overflow`, `pointer-events`, `padding`, `padding-top`, `padding-right`, `padding-bottom`, `padding-left`, `visibility`, and `z-index`.
- Native QSS paint/text fields are resolved by `@quajs/native-ui-compiler` before Rust sees projection JSON. `background-image` is emitted as structured package-relative `asset(...)` metadata in `UiSurfaceResolvedStyle.backgroundImage`, never as browser `url(...)`; `background-size` is a fit subset; `background-position` and `object-position` are origin subsets; `object-position` maps to `ImageDrawParams.origin` for native UI `Image` leaves; `border-style` supports `solid` / `none`; `font-style`, `letter-spacing`, `text-decoration`, `text-overflow`, `text-transform`, and `white-space` map to Rust text/button draw params through resolved style JSON while full text layout, shaping, and richer casing/wrapping remain future text backend concerns.
- `box-sizing` supports `border-box` / `content-box` as compiler-only static bounds normalization. `border-box` preserves the authored QSS fallback width/height as final native surface bounds; `content-box` expands QSS fallback width/height by resolved padding and effective border width before projection. Explicit QUI `width` / `height` props remain authoritative. Rust receives only final resolved bounds and must not parse box-sizing declarations.
- `gap` / `row-gap` / `column-gap` support one or two non-negative logical px / unitless numbers as compiler-only layout metadata that `compileNativeUiSurfaceProjection` expands into Row / Column / Grid direct-child `UiSurfaceNodeProjection.bounds`; Rust receives only resolved child bounds and must not parse gap declarations.
- `justify-content` supports `flex-start` / `center` / `flex-end` / `space-between` / `space-around` / `space-evenly` as compiler-only Row / Column main-axis distribution metadata. `align-items` supports `flex-start` / `center` / `flex-end` as compiler-only Row / Column cross-axis alignment metadata. Both expand to resolved child `bounds`; Rust receives only the final bounds and must not parse alignment declarations or implement flexbox.
- `margin` / `margin-top` / `margin-right` / `margin-bottom` / `margin-left` support one to four non-negative logical px / unitless numbers as compiler-only layout metadata consumed by Row / Column / Grid direct-child projection layout. They expand into resolved child `bounds`; Rust receives only resolved child bounds and must not parse margin declarations or implement a CSS box model.
- `position` supports `relative` / `absolute` as compiler-only layout metadata. `absolute` only removes Row / Column / Grid direct children from structural layout flow and positions them from already-resolved child `bounds` relative to the structural parent; Rust receives only resolved child bounds and must not parse position declarations or implement full CSS positioning.
- `left` / `top` / `right` / `bottom` / `inset` / `width` / `height` are emitted as static `UiSurfaceNodeProjection.bounds` fallback metadata through the TS compiler only. `right` / `bottom` only derive final `x` / `y` when the parent has resolved bounds and the current node has resolved `width` / `height`; `inset` is a non-negative top/right/bottom/left shorthand; `min-width` / `max-width` / `min-height` / `max-height` only clamp those static QSS fallback bounds in the TS compiler; explicit QUI `x` / `y` / `width` / `height` props stay authoritative.
- `display` supports `none` only and is emitted as node-level `UiSurfaceNodeProjection.visible: false` fallback metadata; `overflow` supports `visible` / `hidden` only and is emitted as node-level `UiSurfaceNodeProjection.clipChildren`; `pointer-events` supports node-local `auto` / `none` only and is compiler-only intent metadata where `none` removes that node's own resolved intent without hiding it or disabling child intents; `visibility` is emitted as node-level `UiSurfaceNodeProjection.visible`; explicit QUI `show` props stay authoritative. When no rule overrides visibility, the TS compiler emits `visible: true` explicitly for every node so Rust bridge validation never relies on serde defaults. `padding` is emitted as resolved edge inset metadata in `UiSurfaceResolvedStyle.padding`, and `z-index` is emitted as `UiSurfaceNodeProjection.z_index` rather than stored in `UiSurfaceResolvedStyle`.
- Rust background and object media origin consumption supports resolved keyword origins plus `0%..100%` one-axis or two-axis percentage origins from native UI/QSS tooling, such as `25%`, `25% 75%`, `top 25%`, and `right 75%`. This is projection consumption only; selector parsing, cascade, invalid value diagnostics, and QSS fallback semantics stay in `@quajs/native-ui-compiler` / LSP.
- Do not add QSS selector parsing or CSS cascade logic to `quajs_wgpu_renderer`; add those to the dedicated QSS compiler/language-server/tooling layer and emit resolved projection fields for native rendering.
- Native capability manifests must declare any consumed QSS features and QUI components so runtime packages can check compatibility before activation.
- `native-wgpu.image@1` must declare only image projection/resource support such as background, character, and UI `Image` leaves. Structural QUI nodes such as `Layer` belong to `native-wgpu.ui.surface@1`, not the image capability, so package compatibility cannot treat image support as full UI surface support.
- The `native-wgpu.ui.surface@1` capability must declare the declarative asset kinds it can consume, including `qui`, `qss`, and `tokens`, so dynamic QPK compatibility metadata matches the renderer resource ledger for `UiAst`, `QssStyle`, and `TokenTable`.
- `createNativeUiSurfaceCompatibilityFromDocuments` should derive required `quiComponents` from analyzed QUI component nodes that are registered native-wgpu components only, required `qssFeatures` from analyzed QSS declarations, and required asset kinds from QUI `src` / `image` props plus QSS `background-image: asset(...)`. Imported authoring-only composites such as `Dialog` or `Drawer` must not become required native renderer components unless explicitly passed by the caller for a real official primitive capability. `createNativeUiSurfaceCompatibilityFromProjection` should derive the same compatibility shape from already resolved `NativeUiSurfaceProjection` data without reading QUI/QSS source. Both helpers must call the platform-neutral contracts helper, preserve `nativeCode: false`, and avoid declaring audio/video capabilities unless explicitly provided by the caller.
- Native media capability ids must describe implemented projection/resource contracts precisely. Do not declare QUI media components, real video decode, or audio playback capability until the Rust projection path and backend support exist; fallback-only video/audio capabilities should use deterministic `warn-once` semantics.
- Current UI/QSS resource tracking in `quajs_wgpu_renderer` records package-aware `UiAst`, `QssStyle`, and `TokenTable` entries with deterministic labels and conservative CPU-byte estimates for memory ledger, budget, renderer resource metrics, frame resource sync summaries, package unload summaries, and host cleanup records. Asset planning, frame metrics, renderer resource metrics with package breakdowns, frame resource sync summaries, package release summaries, host cleanup records, `native.memory_ledger.summary.smoke`, `native.package_release.summary.smoke`, `native.frame_resource_replacement.summary.smoke`, and feature-gated `native.wgpu_buffer.heavy_ui_text.smoke` benchmark output identify declarative asset/resource load, release, replacement-cleanup, unload-block pressure, and heavy QUI-style text buffer geometry pressure. Memory-ledger smoke output must include resource-kind, package, declarative-package, and audio-package memory breakdowns so dynamic QUI/QSS/token package resident memory can be benchmarked separately from image/audio/media resources. These are internal resource-ledger estimates only; real host/backend-reported resource memory must be preserved when available.
- Native text keeps a deterministic built-in TextAtlas fallback for unsupported or unavailable product fonts, including ASCII, common punctuation, broad CJK/kana/Hangul/fullwidth slots, decorations, and italic bitmap shear. `@quajs/engine-native` serializes engine-owned `view.plugins.fonts`; `quajs_wgpu_renderer` validates package-aware faces and plans transient `LoadFace`/`ActivateFace`/`ReleaseFace` commands; `quajs_native_app::font_sync` reads bytes only through mounted QPK package candidates. Product frame and teardown paths apply font work in the existing rollback/cleanup boundary, upload package-aware atlas textures, and release them before texture host cleanup. Missing font resource records remain diagnostic ledger signals and must not block deterministic text fallback; texture, video, and `UiAst` source resources remain draw-blocking when required by a submitted frame.
- Real WGPU TextAtlas bind groups prefer matching uploaded decoded family textures and invalidate cached bind groups on upload/release. The native-window producer supports bounded TTF/OTF/WOFF/WOFF2 decode, high-resolution rasterization, Unicode bidi shaping, UAX #14 wrapping, OpenType feature/variation settings, and style/weight face variants. It remains a horizontal visual-novel text backend rather than a full browser layout engine; vertical writing, language-specific hyphenation, high-resolution decorations, and per-cluster cross-face fallback remain outside the current contract.
- Registered native TTF/OTF/WOFF/WOFF2 faces use a dynamic high-resolution atlas path instead of the built-in `5x7` cell geometry. The native-window backend decodes Web font containers with a bounded `64 MiB` expanded-size limit, collects actual frame text, rasterizes glyphs at `64px`, publishes proportional advance/bearing/UV metrics through `FontBackendAtlasLayout`, uploads the RGBA atlas from package-scoped QPK font bytes, and resubmits the same frame with linear TextAtlas sampling. Explicit draw families win; text without an explicit family uses the first active projected face as the transient default. The built-in bitmap atlas is only a fallback when no usable registered atlas/layout exists or a text style is not supported by the high-resolution path.
- High-resolution native text uses a backward-compatible shaping extension on `FontBackendAtlasLayout`: legacy `glyphs: char -> atlas glyph`, top-level `glyphsById`, and the optional shaping face remain available without new required layout fields. Official native backends may attach transient per-family face variants to the shaping face. `rustybuzz` resolves OpenType glyph ids, ligatures, kerning, script clusters, offsets, projected feature settings, and variation settings; `unicode-bidi` splits mixed LTR/RTL paragraphs into visual runs, and `unicode-linebreak` supplies UAX #14 wrapping opportunities. WGPU lays out shaped runs in physical DPR-scaled coordinates, selects face style before nearest weight, retries other faces in the family when the preferred run has missing glyphs, and falls back to the legacy character map when shaping data is absent or incomplete. Shape advances must use the same ascent-to-descent pixel-height basis as `ab_glyph::PxScale`, not UPEM scaling, so enabling shaping does not widen existing UI. Native window smoke reports `shapedTextDrawCount`, and demo smoke requires at least one shaped draw in addition to the existing atlas/bitmap assertions.
- High-resolution atlas geometry consumes projected `fontStyle` and `fontWeight`. One family remains one uploaded texture resource, but each projected face has its own shaping and glyph-id map. Selection prefers exact normal/italic/oblique style, then nearest numeric/normal/bold weight; a selected face lighter than a bold request uses the existing small DPR-scaled mask embolden. This is transient renderer implementation state and must not become font authority outside the engine-owned projection.
- Native window smoke must report cumulative font-atlas uploads from the WGPU backend, final high-resolution atlas text draw count, built-in bitmap text draw count, and final font resource ids. Demo save-preview smoke must fail unless at least one QPK font atlas was uploaded, `fonts:Noto Sans` was used by final text draws, bitmap text draw count is zero, font atlas errors are zero, and the captured PNG remains non-empty at the physical DPR-sized target.
- Real WGPU must use linear filtering for every sampled scene texture, including backgrounds, character sprites, UI images/background images, decoded video frames, placeholders, and font atlases. Nearest filtering is not an acceptable default because it creates a pixel-art appearance when logical-stage content is projected at non-integer scale or high DPR. Native window smoke reports linear and nearest sampled-texture bind-group counts; demo smoke must require at least one linear group and zero nearest groups.
- The native-window encoded image decoder must support QPK JPEG, PNG, and WebP assets for backgrounds, characters, UI images, gallery media, achievement art, and other image projections. Format support must be feature-gated through the native image decoder, covered with real encoded-byte tests, and reported as texture upload failures when decoding fails; do not silently substitute a flat placeholder while claiming the asset rendered.
- CSS/QSS colors are authored in sRGB. Before solid, border, bitmap/font-atlas text, decoration, or shadow colors are written to an sRGB WGPU attachment, convert RGB channels to linear values exactly once and preserve alpha; sampled sRGB textures are decoded by the GPU and must not receive this CPU conversion. Render-graph commands with the same plane and z-index must preserve projection insertion order so parent surfaces paint before later children, matching Web painter order instead of sorting siblings by command id.
- WGPU converts logical bounds to physical pixels with `RenderViewport.physical_scale`; it must apply the same scale to paint metrics that are consumed after that boundary, including font size, line height, letter spacing, text padding, border width, and corner radius. Keep these values logical in engine/QSS projections and scale them exactly once when building physical WGPU primitives.
- Native default dialogue and choice chrome should follow the Web visual-novel theme proportions in logical stage units: 5% safe-area insets, a `12.25%`-of-stage-height compact dark dialogue panel with subtle shadow/border/accent layers, gold speaker hierarchy, and compact right-aligned bordered choices above the dialogue. Dialogue body text is top-aligned inside its content bounds while buttons and compact labels remain vertically centered. Native dialogue speaker projection must fall back from explicit `speaker` to the safe engine-owned `characterName`, matching Web behavior. These are renderer-only draw commands and must not become authoritative UI state.
- Native pointer feedback is renderer-local and transient. Interactive painted `UiButton` / `Panel` commands receive a subtle hover overlay, a stronger pressed overlay, and a persistent focus border from the existing native pointer hit target; overlay-level command-only `UiSurface` shells and painted intent surfaces covering at least 90% of the stage are excluded so full-screen backdrops do not flash or gain a viewport-sized focus ring. Product window paths should dispatch pointer moves and request redraw only when this visual state changes. This feedback must not change the resolved QUI/QSS projection, resource plans, engine/store state, or intent dispatch, and it deliberately does not add QSS pseudo-class parsing or a renderer-owned animation timeline.
- Native form controls follow the Web renderer boundary: range dragging, select open/close/highlight, switch toggling, hover, press, and focus are transient renderer-owned interactions. Resolved UI surface nodes may carry a `control` descriptor with a bounded option list, selected index, visual part ids, and one already-allowlisted semantic intent per final option. Rust updates the temporary control rendering locally and emits only the committed release/change intent; it must not send pointer-move values through QuickJS, understand settings schemas, persist player values, or mutate engine/store state. New engine projections reconcile the temporary selection back to the authoritative selected index.
- Product renderer intents enter the engine through the in-process QuickJS renderer bridge. `RquickJsModuleEvaluator` installs `globalThis.__quaNativeRendererBridge`, `NativeHostPlugin` subscribes the TS QuaEngine pipeline through `installNativeQuickJsRendererIntentBridge`, and `QuickJsRendererIntentHost` dispatches committed Rust intents to that callback. A missing subscriber may queue intents during startup, but product interaction must not depend on Node, JSONL, or filesystem polling. QuickJS callback failures are surfaced as host errors instead of silently duplicating or dropping events.
- Native characters without explicit projected width/height use the Web demo's 1080p standing-sprite reference box: `500x750` logical units scaled from the stage height. Explicit projected dimensions still win. This keeps the same authored character position/scale from becoming a much taller, distorted native sprite while leaving sprite identity and character state engine-owned.
- Current video work in `quajs_wgpu_renderer` supports backend-published decoded frame texture resources but still defaults to poster/fallback rendering when no frame is available: video background projections emit video draw commands with fallback reasons and poster image asset requests, preserve engine-projected playback policy metadata (`loop`, `muted`, `volume`, `playbackRate`, `seekMs`, and `offsetMs`) plus resolved fit/origin styling through native video draw params, and prepared frame/backend submission metrics report fallback counts by video/pipeline/reason plus owner/required package provenance for dynamic QPK attribution. The renderer also plans transient `LoadAsset`/`StartStream`/`UpdateStream`/`StopStream`/`ReleaseDecoder` commands for background video through `NativeVideoBackend`, and `quajs_native_app::video_sync` can resolve `LoadAsset` plans into package-scoped `NativeHostApi::read_asset_bytes` reads using mounted QPK bundle candidates while rejecting unsafe asset/package references before host reads. Product texture/lifecycle frame paths call this resolver only when the injected video backend returns `wants_video_asset_loads()`, pass `VideoBackendAssetLoad` values through `apply_video_asset_loads()`, apply the command plan inside the same rollback boundary as frame preparation so failed package byte reads or backend rejection can retry on a later frame, drain `VideoBackendFrameTexture` and frame texture release outputs, upload decoded RGBA frame textures through the WGPU texture sink with package metadata, and resubmit when uploads succeed. `NativeVideoBackend::video_frame_resources()` may publish current stream `VideoTextureRing` resources; `NativeRendererState` consumes those transient resources on the next frame, `VideoDrawParams.frame_resource_id` lowers to a real textured `VideoFrame` primitive with no fallback reason, and real-wgpu cleanup treats `VideoTextureRing` records as decoded texture cleanup candidates. Package release and renderer clear paths apply video teardown before host texture cleanup when a video backend is present; the `native-window` product path now injects the feature-gated `GifNativeVideoBackend` through `native-video-gif`, requests QPK-owned video bytes, best-effort decodes GIF frames into package-aware RGBA frame textures, advances frames from renderer frame ticks through a transient monotonic clock while honoring projected `loop`, `playbackRate`, explicit `seekMs` updates, and start-only `offsetMs` when no explicit seek is present, and falls back without failing the frame for unsupported bytes or formats. Builds without `native-video-gif` keep `NullNativeVideoBackend`, which does not request loads or publish frame resources. The fallback `native-wgpu.video@1` capability declares consumed QSS features such as `object-fit`, `object-position`, and `opacity` for compatibility checks, but it does not declare QUI media components, MP4/WebM codec support, audio/video sync, codec-level seeking, or full product video playback semantics. Renderer metrics expose `fallbacks_by_owner_package` and `fallbacks_by_required_package`; backend submission diagnostics expose the same provenance per fallback warning. This is not complete real video decode support, and playback policy metadata must not be presented as actual native video playback until a richer decoder/backend consumes it and product smoke/release coverage validates it.
- Native video media teardown cleanup drains pending video frame texture release/upload reports during package release and renderer clear, so package/clear teardown does not depend on a later projection frame to release decoded frame textures.
- Native window smoke reports media-teardown texture outcomes for decoded video frame textures and font atlas textures, including uploaded, released, and error counts, so product smoke output can catch shutdown cleanup drift.
- Native window smoke may persist its already-completed offscreen PNG readback when `QUA_NATIVE_RENDERER_WINDOW_CAPTURE_PATH` points to a `.png` output path. This is a development/E2E artifact output only; it must not load resources, influence rendering, or become game state. Demo smoke clears the old artifact first and validates its byte count and PNG signature against the machine-readable report. `pnpm -C demo native:smoke:scene` produces an unmodified background/character/dialogue/choice reference frame for visual comparison without opening a feature panel or applying an effect.
- `pnpm -C demo native:smoke:parity` produces the same background, standing character asset/position, speaker, and first dialogue line as the Web prologue without native debug HUD or choices. Use it with a 1920x1080 Web prologue screenshot for direct visual comparison; keep `native:smoke:scene` as the broader two-character/choice coverage fixture.
- Native window smoke also reports renderer-local video backend diagnostics for the active product backend. The GIF backend exposes loaded/resident asset counts, applied plan/command counts, active streams, decoded streams, decode failures, missing asset attempts, published frame count, pending frame/release queues, and released texture count. Null backend metrics remain plan/load/active-stream only. These diagnostics are observability for fallback/product parity gaps and must not be treated as real MP4/WebM codec support or authoritative playback state.
- Current audio work in `quajs_wgpu_renderer` is renderer-local resource tracking and backend command planning plus an optional backend asset-load hook: projected tracks may create `AudioBuffer`, `AudioStream`, and `AudioHandle` records, audio asset request plans, cleanup records, memory summaries, unload blockers, audio-specific resource metrics, renderer-local active backend track metrics, and `LoadAsset`/`StartTrack`/`UpdateTrack`/`StopTrack`/`ReleaseHandle` plans for a `NativeAudioBackend`. `@quajs/engine-native` serializes engine-owned `view.plugins.audio` into native `ViewProjection.audio` track projection, including package provenance, normalized renderer-local volume, `stopping` state, and timing/control metadata such as `durationMs`, `fadeInMs`, `fadeOutMs`, `crossfadeMs`, `playAt`, `delayMs`, `seekMs`, and `offsetMs`; Rust JSON facade validation rejects unsafe non-finite, negative, or unbounded audio timing numbers before backend planning. Audio timing/state metadata changes must produce `UpdateTrack` plans rather than asset reloads unless resource identity changes, and `stopping` tracks must stay in renderer resource/backend plans until backend fade-out or cleanup finishes instead of being treated as already stopped. `quajs_native_app::audio_sync` resolves `LoadAsset` plans into package-scoped `NativeHostApi::read_asset_bytes` reads using mounted QPK bundle candidates, records loaded bytes plus bundle/package provenance, rejects unsafe asset/package references before host reads, and refuses scoped fallback when no mounted bundle matches. Product texture/lifecycle frame paths call this resolver only when the injected audio backend returns `wants_audio_asset_loads()`, then pass `AudioBackendAssetLoad` values through `apply_audio_asset_loads()` before applying the command plan; `NullNativeAudioBackend` does not request loads by default. `quajs_native_app` exposes an opt-in `native-audio-rodio` feature that injects a transient rodio output-device backend for native-window product runtimes, decodes package-loaded audio bytes, creates per-track players, applies projected play/pause/stop/volume/loop intent, applies `seekMs` through rodio `Player::try_seek`, uses `offsetMs` as a start-only initial seek when no explicit `seekMs` is present, schedules `playAt` / `delayMs` by pausing until the projected start time, applies renderer-local `fadeInMs` and `fadeOutMs` volume curves, emits standard `audio/ended` renderer intents with reason `natural` or `stopped`, keeps device/player handles renderer-local, and appends `native-wgpu.audio@1` to startup `QuaNativeHostInfo` capabilities only when both `native-window` and `native-audio-rodio` are enabled. This backend must not load filesystem/network audio by itself or mutate engine audio state. BGM crossfade must come from engine-owned audio projection: `@quajs/plugin-audio` keeps outgoing BGM tracks in `bgmOutgoing` with `state: "stopping"` and fade metadata while the new `bgm` fades in, and native consumes those tracks as ordinary package-aware audio projections without renderer-local old-track caches. Default/null/non-window builds must not surface `native-wgpu.audio@1`; future audio capability changes must stay tied to real product-path backend coverage.
- Demo native artifact plans and `demo/scripts/native-dev.mjs` must enable `native-audio-rodio` together. `@quajs/engine-native` maps projected BGM/voice/SFX/ambient tracks to Quack's `audio` asset kind by default so package reads resolve `assets/audio/<assetName>` while preserving the playback kind independently. The generated demo frame projects the existing QPK `demo-native-bgm` track, validates it before launching Rust, and native smoke must require at least one backend plan, the load/start commands, a non-zero peak active-track count, and zero final active tracks after shutdown. A smoke using `NullNativeAudioBackend` or an empty audio projection is not product audio coverage.

## Validation

Prefer light checks while disk is tight:

```bash
pnpm native:verify --no-bench --no-window # includes default Rust runtime plus quickjs-rquickjs runtime/app bridge tests
pnpm -C packages/build/quack exec vitest project-config.test.ts --run
pnpm -C packages/build/quack typecheck
pnpm --filter @quajs/native-contracts typecheck
pnpm --filter @quajs/native-contracts test -- --run
pnpm --filter @quajs/engine-native test -- --run
pnpm --filter @quajs/engine-native typecheck
pnpm -C demo native:smoke:save-preview
pnpm --filter @quajs/assets-native test -- --run
pnpm --filter @quajs/assets-native typecheck
pnpm --filter @quajs/store-native test -- --run
pnpm --filter @quajs/store-native typecheck
pnpm --filter @quajs/native-ui-compiler test -- --run
pnpm -C packages/native/ui-compiler typecheck
pnpm --filter @quajs/native-language-server test -- --run
pnpm -C packages/native/language-server typecheck
pnpm -C packages/native/vscode test -- --run
pnpm -C packages/native/vscode typecheck
pnpm -C packages/native/vscode build
pnpm -C packages/native/benchmarks typecheck
pnpm -C packages/native/benchmarks test
pnpm --filter @quajs/native-benchmarks bench:smoke
cargo test --manifest-path packages/native/Cargo.toml --workspace
cargo test --manifest-path packages/native/Cargo.toml -p quajs_native_runtime --features quickjs-rquickjs quickjs
cargo test --manifest-path packages/native/Cargo.toml -p quajs_native_app --features quickjs-rquickjs quickjs
cargo test --manifest-path packages/native/Cargo.toml -p quajs_wgpu_renderer --features bench-smoke
cargo test --manifest-path packages/native/Cargo.toml -p quajs_wgpu_renderer --features real-wgpu-noop,image-decode noop_device_applies_buffer_upload_and_empty_render_pass
```

Run Cargo only when disk has enough headroom. Check `df -h . $HOME/.cargo` first and clean cargo caches/targets when space is low.

## Review Checklist

- Is state still owned by engine/store/plugins?
- Does native host API avoid arbitrary filesystem, shell, network, dynamic library loading, and Rust callbacks?
- Does Rust `NativeHostInfo` serialize to the TS `QuaNativeHostInfo` shape, including camelCase fields and `debug`/`release` plus `macos`/`windows`/`linux` literals?
- Does the Rust native renderer capability manifest include projection keys, intent events, asset kinds, QSS features, QUI base components, and fallback policy?
- Does the emitted native target-bundle manifest record QuickJS/native runtime/assets-native/store-native versions, and does `@quajs/engine-native` compare them with `QuaNativeHostInfo.runtime` before startup continues?
- Are video/audio native capability declarations limited to currently implemented projection/resource paths, with real decoder/playback backend support guarded by later capability updates?
- Are native renderer version/capability checks performed before dynamic package JS evaluation?
- Does `@quajs/engine-native` install `createNativeRuntimeTrustPolicy` so runtime native-code payload guards run before QuickJS module loading?
- Does `@quajs/engine-native` route runtime package `assetKinds`, `intentEvents`, `qssFeatures`, and `quiComponents` compatibility metadata through the native trust policy before signature verification and QuickJS module loading?
- Does `@quajs/engine-native` use a restricted runtime module loader that reads package script assets and delegates only to the Rust/QuickJS evaluator, without filesystem, network, Blob, or dynamic import paths?
- Do native QuickJS release/summary host APIs use a persistent package-aware namespace registry, and does `@quajs/engine-native` call those APIs only as cleanup/query helpers rather than treating namespace ids as module exports?
- Do assets/store adapters preserve core contracts without Web/Node assumptions?
- Are Web/Cocos/native target core adapters isolated?
- Does the post-bundle dependency manifest prove the active artifact contains exactly one target core plugin set?
- Does packaging use separate Web, Cocos, and native target-core resolver contexts rather than one shared all-target plugin list?
- When packaging Web, Cocos, or native, do core plugins come only from that target's resolver, with no shared all-target resolver, no pre-filtered Web/Cocos/native union, and no target core adapter passed through ordinary plugins, shared presets, Runtime QPK dependencies, debug shells, installers, updaters, or smoke runners?
- Do generated Web, Cocos, and native project templates/startup shells consume only their emitted active-target manifest, without importing, re-declaring, or filtering inactive target core plugins?
- Do Web, Cocos, and native project generators avoid shared cross-target templates that carry multiple target core plugins and filter later?
- Do `targetCoreResolver` and `selectedCorePluginFamily` match the artifact target, selected adapters, renderer entries, and Runtime QPK executable dependencies?
- Does `validateTargetBundleManifest` pass for the emitted Web/Cocos/native artifact, including Runtime QPK executable dependency and renderer entry checks?
- Do `projectGraphs` distinguish template/startup/installer/updater/smoke graphs from post-bundle graphs, rejecting any target core in the former and inactive target core in the latter?
- Is `target-bundle-manifest.json` emitted and validated for both debug and release artifacts after bundling/tree-shaking?
- Do native release target-bundle manifest writes refuse to overwrite a different existing version/platform artifact, while debug manifests remain regeneratable?
- Does runtime startup repeat the exclusive Web/Cocos/native bootstrap assertion before engine initialization?
- Does Rust native app startup validate the emitted target-bundle manifest before constructing host info or starting QuickJS/engine?
- Are target isolation checks applied separately to bootstrap core adapters, renderer/plugin entries, and Runtime QPK compatibility metadata?
- Are Rust-emitted native renderer intents bridged through `@quajs/engine-native` into existing render-to-logic pipeline events, with malformed payloads reported as renderer errors instead of a parallel native event path?
- Does native save-preview capture read a completed transient WGPU frame, report actual encoded bytes/MIME/dimensions through the existing pipeline, fail recoverably without a provider, and avoid advertising capture support on null/headless hosts?
- Does plugin source metadata pass `validateTargetPluginManifest` before Web/Cocos/native entry selection?
- Do shared plugin entries avoid importing target adapters, and do target entries avoid importing other target entries?
- Do Runtime QPKs avoid executable dependencies and renderer entries on Web/Cocos/native core adapters?
- Do bootstrap tests cover Web, Cocos, and native core package sets without cross-target leakage?
- Do exclusive bootstrap tests fail when zero or multiple Web/Cocos/native target core adapter sets are registered?
- Do multi-target plugin fixtures prove only the active Web/Cocos/native renderer entry is bundled?
- Does runtime startup fail when more than one target core adapter set is registered?
- Are runtime package native-code payloads rejected at build time and runtime?
- Are native renderer transient resources tracked in the package-aware resource ledger, including CPU/GPU bytes, dependencies, unload blockers, and cleanup paths?
- If touching audio, is the change still renderer-local resource/projection tracking unless a real backend and capability contract are implemented?
- Do native package unload/release paths use renderer-level unload plans so active frame references block release, and do hosts receive released resource records for native handle cleanup?
- Does Rust QuickJS evaluation register successful module namespace handles in the package-aware namespace registry and release package-owned namespaces during runtime package unload/teardown?
